# 인수인계 — 2026-08-04 · 08-05 · 08-07 세션

이 문서 하나로 다른 PC에서 이어받을 수 있게 쓴다. 제품별 상세는 이미 있는 문서를
가리키고 여기서 되풀이하지 않는다.

| 문서 | 무엇이 있나 |
|---|---|
| **이 문서** | 무엇을 했고 무엇이 열려 있나, 그리고 밟은 함정들. **다음에 할 것은 §7의 ★ 항목** |
| `GOALIE_HANDOFF.md` | Goalie 상세 — B 백로그 전말, A 역산 결과, 감사 도구 |
| `DESIGN_SYSTEM_AUDIT.md` | Parkie 감사 기록. 8절 끝에 이번 아이콘 규격 실측 |
| `PARKIE_MERGE_PLAN.md` | 제품 추가·제거 절차 (14절) |
| `GOALIE_EVIDENCE.md` | Goalie 전달 근거 |

## 0. 다른 PC에서 시작하기

저장소 밖 의존성은 **Chrome 하나뿐이다.**

```
git clone https://github.com/designerkei/HL_Parkie_UI.git
cd HL_Parkie_UI

git config --local user.name  "designerkei"
git config --local user.email "239721783+designerkei@users.noreply.github.com"

npm ci
node tests/server.js &
npm test                    # 108개 통과하면 환경 복구 완료
```

**두 번째 줄을 건너뛰지 마라.** 8월 4일에 이 PC의 전역 git 설정이 회사 도메인 주소였다.
그대로 커밋했으면 공개 저장소에 영구히 남았다. 첫 커밋 전에 `git config user.email`로
확인할 것.

- **Chrome이 있어야 한다.** `playwright.config.js`가 `channel: 'chrome'`이라 번들
  chromium이 아니라 실제 Google Chrome을 쓴다. 없으면 108개가 실행조차 안 된다
- 검증된 기준선은 `node v22.23.1` · `npm 10.9.8`. **`node v26.4.0`에서도 전부 통과하지만
  스위트가 3~4분대다.** 세션 중 스무 번 남짓한 실행이 전부 2.8~4.4분 범위였으니 찬 캐시
  탓이 아니다. **8월 4일 밤 node 22.23.1에서 다시 재서 가름이 났다 — 90개가 1.1분.**
  느린 것은 저장소가 아니라 node 26이다. 3~4분이 나오면 `node --version`부터 볼 것

  **`timeout: 90_000`과 헷갈리지 말 것.** 긴 테스트는 본문에서 각자 `test.setTimeout()`을
  선언한다(axe 33라우트 180초, route-sweep·goalie-audit 600초). 전역 90초가 지키는 것은
  **그 선언이 닿지 않는 fixture 설정 구간**이고, 간헐 실패가 나던 곳이 거기다

- **그 간헐 실패의 정체를 8월 5일에 잡았다. 고쳐지지 않았으니 알고 있어야 한다.**

  ```
  Test timeout of 90000ms exceeded while setting up "context".
  Error: browser.newContext: Test ended.
  ```

  **다섯 번에 한 번꼴로 난다.** 스위트가 57초가 아니라 2.5분대로 뜨고, 스펙 하나가 1.5분
  (= 전역 90초)을 쓰고 죽는다. **죽는 스펙이 매번 다르다** — route-sweep · goalie-audit ·
  cpms-ui · goalie-ui가 돌아가며 걸렸다. 테스트 내용과 무관하고, 마침 그때 context를
  올리던 스펙이 걸릴 뿐이다.

  **변경 탓으로 오인하기 쉽다.** 실제로 한 번 그렇게 의심해서 A/B로 갈랐다 — 변경본
  5회 중 4회, `git stash`로 뺀 원본도 **5회 중 4회**. 같은 비율, 같은 문자열이다.
  **실패가 나면 먼저 이 문자열인지 보고, 맞으면 다시 돌려라.** 다른 문자열이면 그때가
  진짜다
- `.route-baseline/`은 gitignore 대상이라 **§4의 before/after는 한 PC에서 한 세션에**
  끝내야 한다. 다른 PC에서 뜬 before와는 비교할 수 없다
- 라이브: <https://designerkei.github.io/HL_Parkie_UI/>

## 1. 지금 상태

| | |
|---|---|
| 브랜치 | `main` = `origin/main` — **푸시·배포까지 끝났다** |
| HEAD | 08-07 커밋. `git log origin/main..main`으로 확인할 것 |
| 검증 | `npm test` **108개 · 실패 0으로 연속 3회** (6.9 · 5.5 · 5.9분). 3회 중 2회에 `flaky` 1건씩 — §0의 fixture 정체이며 재시도에서 통과했다 |
| 미검증 커밋 | 이 문서를 갱신한 커밋. 문서 전용이라 페이지에 영향 없음 |
| 스펙 파일 | 9개 (`tests/*.spec.js`) |
| 버튼 | **`rgb(0,170,255)` + 흰 글씨 — rest 2.56:1로 기준 미달, 알고 수용한 상태**(§2-3) |
| 라이브 확인 | 08-05 시점: 자산 200, 포커스 링 62%, ZIP 2종 정상, 아카이브 89파일 **엄격 XML 파싱 통과**, boolean 속성 전부 DOM 반영 |

**커밋 = 배포다.** 빌드 단계가 없고 Pages가 `main` 루트를 그대로 서빙한다. 푸시가 곧
공개이므로 공개돼도 되는 것만 올린다.

**작성 시점의 HEAD를 그대로 믿지 말 것.** 해시가 아니라
`git log <마지막 검증>..HEAD` **범위**로 확인한다. 이 저장소에서 이미 한 번, 미검증
커밋을 하나로 적어둔 사이 세 개가 더 쌓인 적이 있다.

## 2. 무엇을 했나

### 2-1. 08-04 세션 — Goalie 백로그 종료 + Parkie 아이콘 (커밋 15개)

#### Goalie (백로그 종료)

| 커밋 | 내용 |
|---|---|
| `31e4eb9` | **B-4** 근거 소멸 — 문서가 잉크가 아니라 채움색을 재고 있었다 |
| `33cbbaf` | `route-baseline.js`가 "every route"라면서 90개 중 88개만 보고 있었다 |
| `63fe7c3` | **B-2** `opacity: 0.3` → 상태별 전용 토큰 + 게이트 |
| `e1b4390` | **B-5** 전달 표면이 이미 레이어였다 — 알파로 되돌림 + 게이트 |
| `1cec34f` | **B-3** 채움은 그대로, 눌리는 상태에 경계 추가 + 게이트 |
| `d4aef26` · `43cfbea` | 인수인계서 갱신 |
| `adf5b7b` | **A-1~A-3 역산** — 셋 다 서술과 실제가 달랐다 |
| `db868b8` | **A-2 포커스 견본** — 계약도 빌드도 이미 갖고 있었고 견본 시트만 빠져 있었다. 겸해서 번역 게이트를 15페이지 전부로 넓히니 한글 295자가 나왔다 |

**B 백로그는 전부 닫혔다. A-2도 닫혔다.** 상세는 `GOALIE_HANDOFF.md` §3.

#### Parkie (아이콘)

| 커밋 | 내용 |
|---|---|
| `69b4d12` | 아이콘 내보내기를 **카테고리별 시트**로 — 도형 38파일 → SVG 5장 ZIP |
| `decb82d` | 아이콘 포커스 링 **3px 40% → 2px 불투명** (2.11:1 → 7.24:1) |
| `2517bc3` | `Refresh` 라이브 영역 지적 철회 — 근거가 틀렸다 |
| `1fa5658` | 아이콘 판 **30 → 32px** (여백 3 → 4px, 4px 격자 정합) |
| `e9863c0` | **타깃 크기 게이트** — `.pk-icon-button` 지적이 실측에서 소멸. 결함이 없는 채로 넣은 게이트다 |
| `b3569fc` | **통신 5상태를 형태 축으로.** 끊김·오프라인이 같은 글리프였다(그룹 설명은 "형태와 색상 두 축"이라고 적혀 있었다). 오프라인 도형 신설, 약함·재연결의 노랑·하늘색을 중립으로, 문서 두 표면을 한 배열에서 파생. 포커스 링 흰 띠 2 → 1px |

상세는 `DESIGN_SYSTEM_AUDIT.md` 8절 — 아이콘 규격과 그 아래 "통신 상태를 형태 축으로 옮긴 것".

### 2-3. 08-07 세션 — Primary rest를 브랜드 블루로 되돌림 (기준 미달 수용)

**08-05의 결정이 뒤집혔다. 접근성 기준을 못 지키는 상태로 출하한다.** 제품이 밝은 브랜드
표면을 되찾기를 원했고 흰 글씨도 유지하기를 원했는데, 그 둘은 동시에 성립하지 않는다.
숫자를 제시했고 제품이 표면을 택했다.

| 상태 | 채움 | 흰 글씨 | |
|---|---|---|---|
| **Rest** | **`brand-500` `#00AAFF`** | **2.56:1** | **미달 — 수용** |
| Hover | `brand-800` `#08638F` | 6.58:1 | 통과 (안 건드림) |
| Pressed | `brand-900` `#0D4F70` | 8.86:1 | 통과 (안 건드림) |

**움직인 것은 rest 하나다.** 라벨은 쉴 때 안 읽히고 손이 닿는 순간 읽힌다. **예전의 밝은
램프는 되살리지 않았다** — 그 hover는 `#16DCF2`로 흰 글씨에서 **1.67:1**이라 hover가 셋 중
가장 안 읽히는 상태가 된다. Goalie 버튼에서 고쳤던 역전과 같은 결함이다.

**08-05가 곁들여 고친 것은 유지했다** — 스피너 트랙, `.pk-robot-command--primary` 토큰
정리, **채움 토큰을 잉크로 읽던 결함**. 채움 값과 무관한 correctness 수정이다.

**게이트를 지우지 않고 항목으로 고정했다.** 상세는 `DESIGN_SYSTEM_AUDIT.md` 8절 첫 항목:

- axe 스윕은 `color-contrast` 중 **흰색 on `#00aaff`** 조합만 통과시킨다. 셀렉터가 아니라
  색 조합으로 고정 — axe가 `button[data-dc-tpl="542"]`로 보고하는데 템플릿 생성 값이다
- 대비 표 게이트는 "미달 0개"에서 **"미달은 정확히 Primary rest 한 줄, 값은 2.56:1"** 로
- **예외가 실제로 쓰이는지도 단정한다.** 채움이 통과 색으로 돌아가면 그 단정이 실패해
  "예외를 지우라"고 알린다
- 변이 3종 검증: 채움을 통과 색으로 되돌리기 · 다른 곳에 두 번째 대비 결함(표 게이트) ·
  같은 결함(axe 필터). 전부 재시도까지 실패했다 — **예외가 다른 결함을 삼키지 않는다**

**3회 검증이 누락을 잡았다.** 처음에 `design-system-audit.spec.js`의 axe 스윕만 고쳤는데
`multi-system.spec.js`에 **두 번째 Parkie 스윕**이 있어 268건으로 터졌다. 변이 검증 3종을
전부 통과한 뒤에 나온 실패다 — 변이는 내가 고친 게이트가 제대로 잡는지만 확인하고,
**내가 고치지 않은 게이트가 있는지는 확인해주지 않는다.**

**되돌리려면** `--parkie-action-primary-bg`를 `brand-700`으로, 두 게이트의 예외를 지우고,
버튼 페이지의 `[data-action-contrast-note]`를 고친다. 같은 `#00AAFF` 위 어두운 잉크
`#062226`은 6.42:1로 통과하므로, 밝은 표면과 기준을 함께 지키는 길은 그쪽뿐이다.

### 2-2. 08-05 세션 — 컴포넌트 SVG 내보내기 + 색 결정 두 건 되돌리기 (커밋 13개)

| 커밋 | 내용 |
|---|---|
| `663d6b4` | 아이콘 포커스 링 **불투명 → 62% 알파** (7.24:1 → 3.45:1). `decb82d`를 되돌린다 — 대비 계산은 옳았지만 7.24:1은 링이 아니라 불 켜진 테두리로 읽혔다. 면적은 SC 2.4.13이 잠가두었으므로 알파가 유일하게 남은 다이얼. 게이트를 `alpha === 1`에서 **양쪽 괄호**(투명 상한 · 3:1 하한)로 교체 |
| `9f27074` | **액션 채움을 한 단 내리고 흰 글씨.** `brand-700/800/900` = 4.55/6.58/8.86:1. 새 색 없음. hover가 어두워지는 것은 취향이 아니다 — 흰 글씨가 4.5:1을 넘는 가장 밝은 파랑이 `#007CBA`라 올릴 여유가 없다. axe가 `.pk-robot-command--primary`의 **채움 토큰을 잉크로 쓴 결함**을 잡았다 |
| `28044b9` | **체크박스·라디오를 CSS로 그린다.** `accent-color`뿐이라 직렬화할 도형이 없었다 — 내보내기의 전제. `--parkie-control-border` 신설(4.53:1), forced-colors 대비 포함 |
| `b18e5b9` | **DOM→SVG 엔진** `export/dom-to-svg.js`. pseudo-element 승격 후 실측, transform 재적용, 원형 테두리 4분원 |
| `b696e88` | 버튼 페이지 내보내기 + 공용 툴바 + `tests/specimen-export.spec.js` |
| `31db79b` | 세그먼트·선택 컨트롤 내보내기 |
| `548eaee` | 남은 8개 페이지. `<img>` 데이터 URI 임베드(다운로드 경로가 async가 된 이유) |
| `5c11a34` | **전체 묶음** — 다운로드 페이지가 placeholder에서 실제 페이지로. 목적지 33 → 34 |
| `cd0ade1` | **게이트 재조준 3건** — 변이 14종 중 3종이 살아남았다 |
| `f37e1e8` | 표본 크기를 **그리는 것의 합집합**으로. 사이트 셀렉트 팝오버가 행을 넘어 그려지고 있었다 |
| `93c1a8d` | CPMS axe 스윕의 경쟁 조건 — `goto` 직후 axe를 돌려 include 대상이 없을 때가 있었다 |
| `ee0b9aa` | **값 없는 boolean 속성이 DOM에 닿게.** 런타임이 `disabled`의 값 `""`를 그대로 넘겨 React가 false로 읽었다. 16곳이 무력했다 — 포커스·클릭되는 "Disabled" 버튼, 타이핑되는 "Disabled input example", 빈 체크박스 7개. `collectProps`에서 값 없을 때만 `true`로 강제. 90경로 baseline diff 차이 없음 |
| `2440db6` | **배포 후 수정 — 89개 중 84개가 잘못된 XML이었다.** `esc()`가 인용부호를 처리하지 않아 계산된 `font-family`(`Pretendard, "Noto Sans KR", …`)가 속성을 닫아버렸다. **브라우저와 Figma는 관대하게 파싱해서 전부 정상으로 보였다** — 잡은 방법은 라이브 아카이브를 엄격한 파서로 돌린 것. 게이트가 이제 `DOMParser`로 전부 파싱한다 |

**08-05 세션에서 가장 값비쌌던 교훈** — 변이 검증 14종을 통과하고 3회 연속 통과로
배포한 **뒤에** 89개 중 84개가 malformed XML임을 발견했다. 세션 내내 스크린샷으로
확인했고 전부 정상으로 보였다. 브라우저와 Figma가 관대하게 파싱하기 때문이다.
**"열어봤는데 정상이었다"는 관대한 파서의 판정이지 검증이 아니다.** 산출물의 계약이
있으면(SVG는 XML이다) 그 계약대로 검사해야 한다. 상세는 `DESIGN_SYSTEM_AUDIT.md` 12절 끝.

**요청은 세 가지였다** — 포커스 링 감광, 파란 버튼 흰 글씨, 11개 컴포넌트 SVG 내보내기.
앞의 둘은 저장소가 테스트·본문으로 잠가둔 결정을 되돌리는 일이었으므로 근거 기록까지
같이 고쳤다. 상세는 `DESIGN_SYSTEM_AUDIT.md` 8절의 세 항목과 12절 끝.

**값 없는 boolean 속성 16곳 — 런타임에서 고쳤다.** DC 런타임이 값 없는
`checked`·`disabled`를 버려서 "Disabled" 버튼 견본이 포커스·클릭이 되고 체크박스 7개가
빈 상태로 렌더링됐다. `support.js`의 `collectProps`에서 알려진 boolean 속성이 값 없이 올
때만 `true`로 강제한다. 90경로 `route-baseline` diff는 **차이 없음**, 게이트는 개수를 재고
강제를 끄는 변이로 검출을 확인했다.

**처음 적었던 "57곳"은 틀린 숫자였다.** `grep`이 `aria-disabled`·`is-selected`·JS
변수명까지 세고 있었다. 태그를 실제로 파싱하니 16곳이고 Goalie·Skeleton은 0곳이다.
**어림 grep은 계량이 아니다** — §3의 "재라"가 이 문서에도 적용된다.

## 3. 일하는 방법 — 이게 가장 중요하다

**문서에 적힌 항목을 착수 전에 다시 재라. 08-04에 일곱 번, 08-05에 두 번 더 뒤집혔다.**

| 항목 | 문서가 말한 것 | 실측 |
|---|---|---|
| Goalie B-1 | "채움을 어둡게 해야 풀린다" | 이미 4.67:1로 통과 중 |
| Goalie B-4 | "라벨 대비 미달 3.51 / 3.34" | 잰 것이 잉크가 아니라 채움색. 실제 6.57 / 4.88 |
| Goalie A-1 | "빨강이 일반 버튼 색이기도 하다" | 제품에서 이미 비상 전용 (danger 버튼 2개, 둘 다 비상 해제) |
| Goalie A-2 | "포커스를 신설해야 한다" | 빌드에 25곳, 계약에도 선언돼 있었다. 견본 한 줄만 없었다 |
| Parkie #2 | "`Refresh`가 라이브 영역 초과" | Fluent가 전 사이즈에서 그렇게 그린다. 채택본이 그중 최선 |
| Parkie `.pk-icon-button` | "제품의 **진짜 컨트롤**이 24×24라 영향이 크다" | 5회 전부 컴포넌트 무대 위 견본이고 크기 변형 4종은 아무 데도 안 쓰인다. 24×24는 턱걸이가 아니라 2.5.8 통과 |
| Parkie 아이콘 규격 #3 | "작은 아이콘 **7개**를 다시 그려야 한다" | 4개다. `More`·`Filter`는 MS 채택본이고 `Route`는 이미 18×18에 맞는다. 게다가 적힌 숫자가 전부 획이 빠진 값이었다 |
| Parkie 포커스 링 (08-05) | "알파는 잘못된 다이얼이다" (`decb82d`가 본문·게이트에 적어둠) | **알파가 유일하게 남은 다이얼이었다.** 면적은 SC 2.4.13이 잠가두었으므로. 게이트가 `alpha === 1`을 단정해 "너무 밝다"를 잡을 수 없는 방향으로 잠가둔 상태였다 |
| Parkie boolean 속성 (08-05) | "58곳" (내가 `grep`으로 세어 이 문서에 적었다) | **16곳.** `grep`이 `aria-disabled`·`is-selected`·JS 변수명까지 셌다. Goalie의 "62개"는 한 개도 속성이 아니었다 |

**여섯 번째는 이 문서가 틀린 경우였다.** §7에 "영향이 크다"고 적어둔 항목이 다음 착수에서
실측 한 번에 소멸했다. 인수인계서에 적혀 있다는 것도 근거가 아니다 — **여기 적힌 것을
포함해서** 재라.

**값을 바꾸기 전에 그 값이 어떤 규칙의 결과인지 역산하라.** 전달자는 대개 규칙을 갖고
결과만 납품한다. Goalie B-2·B-3·B-5가 전부 **전달값을 하나도 바꾸지 않고** 닫혔다:

- `#B3EEFE` = enabled 채움의 정확히 **30% 틴트** → 같은 규칙을 빨강에 적용
- `#F9D6D4` = pressed 채움의 정확히 **20% 레이어** → 알파로 되돌리니 픽셀 동일
- 램프가 호버에서 밝아지는 건 의도 → 채움은 두고 경계를 추가

**다른 시스템의 잣대를 그대로 대지 마라.** Parkie #2가 그래서 틀렸다. Material의 20dp
라이브 영역 규칙을 Fluent 채택본에 들이댔는데, Material 자신도 얇은 링이 같은 크기로
보이려면 더 커야 해서 원(⌀20)을 정사각(18×18)보다 크게 잡는다.

## 4. 밟은 함정 — 다시 밟지 말 것

**같은 검사가 여러 곳에 있다. 하나만 고치면 나머지가 터진다.**
`AxeBuilder`는 스펙 파일 **다섯 곳**에서 돌고, 그중 Parkie 전체를 훑는 것이 둘이다
(`design-system-audit.spec.js:104`, `multi-system.spec.js:509`). 08-07에 앞의 하나만
고쳤다가 뒤의 것이 268건으로 터졌다. 게이트를 완화하거나 예외를 넣을 때는
**`grep -n "new AxeBuilder" tests/*.spec.js`로 전수를 먼저 세라.** 같은 조언이
`toHaveCSS` 색 단정과 `route-baseline`에도 적용된다 — 한 값이 여러 곳에 박혀 있다.

**`getBBox()`는 그 요소 자신의 `transform`을 적용하지 않는다.**
MS 채택 아이콘은 전부 path에 `transform="matrix(1 0 0 1 3.25 1.25)"`가 붙어 있다.
path 단위로 재면 **크기는 맞지만 원점과 여백이 틀린다.** `Refresh` 여백을 1.25가 아닌
0.00으로 읽고 그 값으로 결론을 냈다. **`<svg>` 루트에서** 부르면 자식 transform이
적용된 합집합이 나온다.

**`getBBox()`는 획도 포함하지 않는다 — transform과는 별개의 실수다.**
루트에서 부르면 transform은 해결되지만 획은 여전히 빠진다. Parkie 도메인 아이콘은
`stroke-width="2"` · `fill="none"`이라 **실제 잉크가 각 변 +2**인데, MS 채택본은 채움이라
잉크와 기하가 같다. 한 카탈로그에 두 종류가 섞여 있으므로 **같은 자로 재려면 렌더 후
칠해진 픽셀**을 재야 한다(40배 래스터라이즈 → 알파 있는 픽셀의 경계 상자). 아이콘 규격
#3의 숫자 일곱 개가 이것 때문에 틀렸다.

**`box-shadow`에서 숫자를 긁으면 오프셋을 알파로 읽는다.**
`getComputedStyle`이 `rgb(0, 170, 255) 0px 0px 0px 2px`를 돌려주므로, 문자열 전체에서
`[\d.]+`를 뽑으면 네 번째 값이 알파가 아니라 첫 오프셋 `0`이다. 불투명 링이 alpha 0으로
보고된다. **색 함수만 따로 떼어내야 한다** — `icons/svg-export.js`의 `ringOf()`가 그 방식.

**명시도가 같으면 선언 순서로만 이긴다.**
B-3의 새 규칙은 기존 규칙과 명시도가 같아서 **뒤에 오는 것만으로** 이긴다. 위로 옮기면
조용히 예전 동작으로 돌아간다. 게이트로 잡는지 실제로 확인했다.

**테스트 하네스는 `reducedMotion: 'reduce'`로 돈다.**
`playwright.config.js` 설정이다. 애니메이션을 넣고 `prefers-reduced-motion` 분기를
빠뜨려도 **테스트에서는 드러나지 않는다.** Goalie에 첫 `@keyframes`를 넣을 때 특히 주의.

**`esc()`가 인용부호를 처리하지 않으면 SVG가 malformed XML이 된다.**
계산된 `font-family`는 `Pretendard, "Noto Sans KR", …`이고 그 인용부호가 자신을 담은
속성을 닫는다. **브라우저와 Figma는 관대하게 파싱해서 그대로 렌더링한다** — 89개 중
84개가 깨진 채로 모든 스크린샷 검사를 통과했다. 산출물에 계약이 있으면(SVG는 XML이다)
**엄격한 파서로** 검사할 것.

**변이 하네스에서 `git checkout --`을 쓰면 커밋 안 한 수정이 날아간다.**
변이를 되돌리려고 `git checkout -- <file>`을 넣었는데, 그 파일에 아직 커밋하지 않은
진짜 수정이 있었다. 테스트만 남은 커밋을 만들고 게이트가 3회 연속 실패했다. **변이는
원본 문자열을 메모리에 들고 있다가 되쓰는 방식으로 되돌릴 것.**

**`grep`으로 속성을 세지 마라.** `grep -oE '(disabled|selected)'`가 `aria-disabled`,
`is-selected`, `--state-disabled`, JS 변수명, 본문 산문까지 센다. 값 없는 boolean 속성을
58곳이라고 기록했는데 실제로는 16곳이었고, Goalie의 "selected 62개"는 **한 개도 속성이
아니었다.** 태그와 속성을 파싱해서 셀 것.

**전역 git 설정이 회사 이메일일 수 있다.** §0 참고.

## 5. 게이트를 추가할 때

**통과하는 테스트는 그 자체로 증거가 아니다.** 새 게이트마다 **지키겠다는 대상을 파괴해
실패를 확인**한다. 08-04에 게이트 10개 · 변이 34종, 08-05에 게이트 3개 · 변이 17종을 돌렸다.

**살아남으면 먼저 조준을 의심하라.** 한 번 빗나갔다 — `resolveToken`을 빼서 "`var(`가
남으면 안 된다"를 검증하려 했는데 살아남았다. `resolveVars`가 resolver가 없으면 fallback
으로 치환해서 애초에 `var(`가 남을 수 없는 구조였다. 게이트가 약한 게 아니라 변이가 다른
것을 건드린 것이라, 강화하지 않고 사실만 기록했다.

**깨끗한 페이지에서는 거부 경로가 아예 돌지 않는다.** 타깃 크기 게이트가 그랬다. 페이지에
결함이 없으니 간격 예외를 꺼도 아무 변화가 없어 변이가 살아남았다. 붐비는 쌍을 심어
해결한 줄 알았는데 **탐침이 검출 로직을 따로 복제해 갖고 있어서**, 한쪽을 변이시켜도
다른 쪽이 통과했다. **구현은 하나여야 하고 실제 페이지와 탐침이 그 하나를 같이 써야 한다.**
그래야 예외를 어느 쪽에서 약화시켜도 실패한다.

**성질을 재고 값을 재지 마라.** B-3 게이트는 색이 아니라 "채움과 경계 중 큰 값이
enabled → hover → pressed 순서로 줄어들면 안 된다"를 잰다. 램프 재조율은 자유롭게 두고
역전만 막는다. 포커스 게이트도 판 크기를 빌드에서 읽어 WCAG 하한을 계산하므로,
30 → 32로 판이 바뀌었을 때 **손댈 것이 없었다.**

**검사가 실제로 돌았는지도 단정하라.** 이 저장소에는 결함이 고쳐지자 대상 집합이 비어
게이트가 조용히 게이트를 그만둔 전례가 있다.

**개수·집합 크기로 "있음"을 증명하지 마라. 08-05에 네 번 걸렸다.**

| 약한 단정 | 왜 통과했나 | 고친 방식 |
|---|---|---|
| 체크된 박스가 빈 박스보다 도형이 많다 | 체크마크가 없어도 판+테두리 2 > 1 | 잉크 색(`--parkie-brand-on`)이 파일에 있는지 확인 |
| 스위치가 색을 2개 이상 쓴다 | 트랙과 노브가 **같은 hex**(불투명도만 다름), 라벨 텍스트로 이미 2개 | 기하로 — 트랙은 넓은 사각, 노브는 정사각 |
| 속성이 있는 요소는 프로퍼티도 참이다 | 강제가 깨지면 React가 속성을 안 붙여 **대상 집합이 빈다** | 페이지가 문서화한 **개수**를 단정 |

**없어졌을 때 값이 달라지는 성질을 직접 재라.** 대리 지표는 사라진 것을 못 본다.

## 6. 검증 절차

```
node tests/server.js &

npm test  ×3                                    릴리스 기준
node tests/tools/goalie-audit.js                Goalie 결함 — 지금 5건
node tests/tools/route-baseline.js before       변경 전 (반드시 먼저)
#   … 변경 …
node tests/tools/route-baseline.js after
node tests/tools/route-baseline.js --diff before after
```

**`route-baseline`이 무엇을 기록하는지 알고 쓸 것.** 제목·테마·h1·내비 수·크롬 위치 같은
**IA와 구조**를 기록하며 **컴포넌트 색은 기록하지 않는다.** 색만 바꾼 변경에서
`no differences`는 "구조가 안 깨졌다"는 뜻이지 "색이 맞다"는 뜻이 아니다. 색은 실측과
게이트로 따로 확인한다.

**`route-baseline`은 `disabled`·`checked` 같은 DOM 상태도 기록하지 않는다.** boolean 속성
수정에서 90경로 `no differences`가 나왔지만 그것은 "구조가 안 깨졌다"는 뜻이고, 속성이
실제로 붙었는지는 별도 탐침으로 확인했다.

**산출물은 그 산출물의 계약대로 검사한다.** 내려받은 SVG는 브라우저로 열어보는 것이
아니라 **엄격한 XML 파서**로 파싱해야 한다 — 브라우저·Figma는 깨진 파일도 렌더링한다.
`tests/specimen-export.spec.js`가 `DOMParser`의 `image/svg+xml`로 전부 파싱한다.

배포 후 라이브에서 확인하고 **배포 커밋 = 통과 커밋**을 지킨다. **라이브에서 실제로
내려받아** 아카이브를 열어볼 것 — XML 결함을 잡은 방법이 그것이었다.

## 7. 열려 있는 것

**우선순위는 Parkie의 ★ 항목이다 — 지도 마커·아바타 흰 글씨 통일.** 사용자가 08-05에
승인했으므로 결정을 다시 받을 필요 없이 착수해도 된다. 다만 **잉크만 바꾸면 대비가
2.56:1로 떨어진다**는 것이 이 작업의 핵심이니 그 항목을 먼저 읽을 것.

**그 외 큰 것은 착수 전에 결정을 하나씩 요구한다.** 승인 없이 바로 할 수 있는 것은
**배터리 톤 클래스 이름 정리**와 **버튼 페이지 콘솔 경고 7건**뿐이고, 둘 다 값은
움직이지 않는다.

**A-3은 조사가 끝나 있다 — 다시 뒤지지 마라.** 후보 5개를 추려 셋을 근거와 함께 제외했고
추천 하나와 함께 정해야 할 축 문제까지 `GOALIE_HANDOFF.md` §3-A "후보 조사"에 적어뒀다.
받아야 할 답은 **두 개**뿐이다: ① 컨트롤을 `해제하기`로 할지, ② 로딩을 버튼 상태로 볼지
도메인 축 값으로 볼지.

### Goalie

| | 상태 |
|---|---|
| A-3 로딩 | **답 두 개만 받으면 착수 가능.** 조사 완료 — 추천은 `해제하기`(Templates 운영 화면, 2곳). **Goalie에는 명령을 보내는 컨트롤이 하나도 없다**(핸들러 28곳 전부 로컬 토글)는 것이 조사의 핵심이라, 스피너 이식이 아니라 계약 문장 문제다. 상세 `GOALIE_HANDOFF.md` §3-A |
| A-1 빨강 | 축은 이미 갈려 있고 값만 공유한다. **삭제 계열 버튼이 생기는 시점이 결정 시점** |
| 열린 결함 5건 | 시안 `#00B4ED`가 스위치 트랙 2.40:1 · 선택 라디오 2.19:1. 전달값이라 문서화만 |
| `wip/iconography-rewrite-2026-08-03` | 새 도형 7종만 가져올지 결정 (`GOALIE_HANDOFF.md` §8) |

### Parkie

#### ★ 다음에 할 것 — 지도 마커와 아바타를 흰 글씨로 통일

08-05에 **버튼만** 흰 글씨로 바꿨다. 파란 배경에 어두운 잉크가 남은 곳이 두 군데 있고,
사용자가 이것도 통일하기로 했다.

| 대상 | 파일 | 지금 |
|---|---|---|
| 지도 로봇 마커 | `components/operations.css:962·966` | `color: var(--parkie-brand-on)` + `border: 2px solid var(--parkie-brand-on)`, 배경 `--parkie-brand-primary` |
| 아바타 견본 5개 | `index.html:2667-2670, 2676` (인라인 `style`) | `color:var(--parkie-brand-on)`, `background:var(--parkie-brand-primary)` |

**잉크만 흰색으로 바꾸면 안 된다. 그것이 이 작업의 전부다.**

| | 대비 |
|---|---|
| 지금: `#06222E` on `#00AAFF` | **6.42:1** — 통과 |
| 잉크만 흰색: `#FFFFFF` on `#00AAFF` | **2.56:1** — AA(4.5) 미달, 큰 글씨(3:1)도 미달 |
| 채움까지 내림: `#FFFFFF` on `#007CBD` | **4.55:1** — 통과 |

아바타 글자는 **이름 이니셜(`한`, `PK`)이므로 텍스트**다. 1.4.3 AA가 그대로 적용된다.

**08-07에 이 항목의 전제가 바뀌었다. 착수 전에 반드시 읽을 것.** 위 표의 마지막 줄
("채움까지 내림 → 4.55:1")이 더 이상 성립하지 않는다. `--parkie-action-primary-bg`가
`brand-700`에서 **`brand-500`(`#00AAFF`)으로 되돌아갔기** 때문이다(§2-3). 배경을
액션 토큰으로 옮겨도 이제 `#00AAFF`에 도착하므로 **2.56:1 그대로다.**

즉 지금 선택지는 셋뿐이다:

| 방법 | 결과 |
|---|---|
| 어두운 잉크 유지 (지금) | 6.42:1 통과. **아무것도 안 하는 것이 기준상 가장 낫다** |
| 흰 잉크 + `#00AAFF` | 2.56:1. 버튼 rest와 같은 미달을 두 곳 더 만드는 일 |
| 흰 잉크 + 배경을 따로 한 단 내림 | 통과하지만 **버튼 rest와 색이 달라진다** — 통일하려던 목적과 어긋난다 |

**그러므로 "버튼에서 한 것과 같은 이동"은 더 이상 답이 아니다.** 시각적 통일을 택하면
미달 두 곳이 늘고, 기준을 택하면 버튼과 달라진다. 착수 전에 사용자에게 이 셋을 숫자와
함께 제시하고 정할 것 — 08-07에 버튼에서 한 것과 같은 방식이다.

지도 마커의 `border: 2px solid var(--parkie-brand-on)`도 함께 봐야 한다. 이 테두리는
마커를 지도 배경에서 떼어내는 역할이라 **어두운 색인 것이 의도일 수 있다.** 흰색으로
바꾸면 밝은 지도 타일 위에서 사라진다. 마커가 얹히는 배경을 먼저 재고 결정할 것.

**착수 전에 확인할 것**

1. 지도 타일 배경색을 실측하고, 마커 테두리를 흰색으로 바꿔도 되는지 판단
2. axe 34라우트 스윕 — 아바타는 `role="img"` + `aria-label`이라 axe가 텍스트 대비를
   보지 않을 수 있다. **게이트가 안 잡아도 결함은 결함이다.** `colorProbe()`로 직접 재라
3. 바꾼 뒤 `--parkie-brand-on`의 소비자는 체크박스 체크마크·라디오 점
   (`controls.css:417-418`)과 경고 대비 카드만 남는다. **토큰 이름이 "브랜드 위 잉크"인데
   브랜드 표면에 아무도 안 쓰는 상태**가 되므로 이름을 다시 볼 것

#### 나머지

| | 상태 |
|---|---|
| 아이콘 규격 #3 | **작은 아이콘 4개** (7개가 아니다 — 다시 재서 셋이 빠졌다). `Charger` 13×13 · `Robot` 16×15 · `Obstacle` 18×12 · `Vehicle transport` 18×14. **도형을 다시 그려야 한다.** 착수 전에 각 아이콘 형태를 보고 Material 키라인(정사각 18 · 원 ⌀20 · 세로 16×20 · 가로 20×16) 중 무엇을 기준 삼을지 정할 것 — 전부 18로 밀어넣으면 `Refresh`에서 한 실수를 되풀이한다. 상세와 정정 근거는 `DESIGN_SYSTEM_AUDIT.md` 8절 3번 |
| 연결 끊김의 빨강이 두 벌 | 아이콘은 `--parkie-connection-lost`(= `--parkie-status-danger`), 상태 배지 `.pk-status--lost`는 `--parkie-danger-text`(= `red-300`)다. **일부러 통일하지 않았다** — 배지는 글자라 밝은 빨강이 필요하고, 묶었다가 대비가 내려갈 수 있다. 통일하려면 먼저 재라 |
| 배터리 톤 클래스 3개가 한 값 | `.pk-domain-icon`의 `is-battery-full`·`is-battery-critical`·`is-charging`이 전부 `--parkie-icon-battery-full`로 풀린다. **동작은 맞다** — 윤곽은 중립이고 의미색은 SVG 안쪽 채움이 갖는다. 이름만 거짓이라 하나로 합치거나 이름을 고칠 것 |
| 버튼 페이지 콘솔 경고 7건 | `[dc-runtime] {{ row.state }} never resolved` 등. **저장소 기존 문제이고 이번 변경과 무관하다**(변경 전후 A/B로 확인). 대비 표가 rAF 토큰 탐침 뒤에 채워지므로 첫 렌더에서 `sc-for`가 미해소 행을 그린다. 고치려면 `buttonSpecs`가 없을 때 `sc-for`를 아예 렌더하지 않게 할 것 |

타깃 크기는 이제 게이트가 지킨다 (`design-system-audit.spec.js`). 지금 페이지는 전부
통과하지만, **결함이 없다고 게이트를 뺄 것은 아니다** — 넣은 이유가 그것이었다.

#### 컴포넌트 SVG 내보내기 — 알려진 한계 (결함이 아니다)

고치려 들기 전에 읽을 것. 셋 다 확인하고 남긴 것이다.

| | 왜 |
|---|---|
| 애니메이션이 첫 프레임에서 멈춘다 | 로딩 스피너·재연결 스트림이 정지 호로 나온다. 측정 시 `animation: none`을 넣으므로 결정적이다. 회전하는 SVG를 원하면 `<animateTransform>`을 따로 만들어야 한다 |
| `filter()`가 빠진다 | SVG 페인트에 대응물이 없다. 유일한 사용처가 스위치 hover 밝힘(`controls.css:534`)이고 내보내는 상태에 hover가 없다 |
| 이벤트 피드 여러 줄 행의 텍스트가 잘려 보인다 | **충실한 재현이다.** 라이브 행도 `overflow:hidden` + `white-space:normal`로 같이 자른다. 내보낸 표본은 448×70, 라이브 요소도 448×70이고 경계를 넘는 텍스트가 없다. 실측으로 확인했다 |

### 제품과 무관하게 열려 있는 것

| | 상태 |
|---|---|
| **fixture 간헐 실패** | `Test timeout of 90000ms exceeded while setting up "context"`. **다섯 번에 한 번**, 죽는 스펙은 매번 다르다. 원본과 변경본 A/B로 갈라 **저장소 기존 문제**임을 확인만 하고 고치지 않았다. 상세와 대처는 §0. 고치려면 Playwright 브라우저 재사용·`workers` 설정부터 볼 것 |

## 8. 함께 알아야 할 것

- **병행 작업이 있다.** 여러 작업자가 동시에 편집한다. 각 단계 전 `git fetch` +
  `git status`로 확인하고 커밋을 미루지 말 것. 미완성 상태가 배포된 전례가 있다
- **커밋 = 배포.** 공개돼도 되는 것만 커밋. 개인 이메일·내부 주소가 문서에 들어가지
  않게 할 것 — 이번에도 인수인계서에 회사 주소를 적었다가 푸시 직전에 뺐다
- 제품 추가·제거는 `PARKIE_MERGE_PLAN.md` 14절
