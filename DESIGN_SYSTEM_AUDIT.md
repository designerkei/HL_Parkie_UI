# Parkie RMS Design System — Completion Audit

Date: 2026-08-01

Product: HL Robotics Parkie 주차로봇 관제 시스템

Scope: Parkie 33개 목적지 전체. `MS 참조`는 비교 자료로만 유지하며 Parkie 완성도 집계에서 제외한다.

## 1. 최종 설계 결정

RMS는 정보 구조, 컴포넌트 구조, 타입 위계, 화면 밀도와 KO/EN 문서 체계를 제공한다. Parkie는 HL Robotics 브랜드, Pretendard·Roboto Mono, 다크 토큰, 상호작용 상태, 주차로봇 도메인 아이콘과 운영 의미 체계를 제공한다.

제품 화면의 색은 다음 우선순위를 따른다.

1. 정상 운행·정상 연결·정상 배터리는 중립색이다.
2. Brand blue는 선택, 포커스, 주요 행동과 현재 경로에만 쓴다.
3. Green은 완료·복구·실제 충전처럼 긍정 의미가 명확한 경우에만 쓴다.
4. Muted cyan은 정보·재연결, Yellow는 주의, Red는 고장·중단·비상에 쓴다.
5. 색만으로 상태를 전달하지 않고 텍스트, 아이콘, 수치, 위치를 함께 제공한다.

## 2. 외부 기준 비교와 반영

| 기준 | 확인한 원칙 | Parkie 반영 |
| --- | --- | --- |
| [Apple Accessibility](https://developer.apple.com/design/human-interface-guidelines/accessibility/) / [Color](https://developer.apple.com/design/human-interface-guidelines/color) | 색 외 단서, 충분한 대비, 읽기 순서와 명확한 조작 결과 | 상태마다 텍스트·수치 병기, 12px 최소 캡션, 33개 화면 axe 검사, 다크 잉크 Primary |
| [Apple SF Symbols](https://developer.apple.com/design/human-interface-guidelines/sf-symbols) | 일관된 그리드·무게·의미, 플랫폼에서 학습된 기호 재사용 | 24px 그리드, Parkie Original·Custom·Adopted 출처, 범용 기호는 Adopted |
| [Material 3 States](https://m3.material.io/foundations/interaction/states/state-layers) | Hover·Focus·Pressed와 선택 상태를 분리 | Default·Hover·Focus·Pressed·Selected/On·Disabled 6상태 매트릭스 |
| [Material Symbols](https://developers.google.com/fonts/docs/material_symbols) | 가변 아이콘 시스템의 일관된 크기·스타일 | 아이콘 24px 기준과 currentColor 토큰 소비 |
| [Meta accessibility co-design](https://about.fb.com/news/2024/07/building-accessibility-into-our-mixed-reality-products/) | 접근성을 사후 체크가 아닌 설계·검증 과정에 포함 | 문서 계약에 접근성 항목 고정, 자동검사·키보드·시각검사를 같은 릴리스 게이트로 운영 |
| [ISA High Performance HMI](https://www.isa.org/getmedia/06130a38-f7af-4b35-8c9c-2c34f25c1977/The-High-Performance-HMI-Overview-v2-01.pdf) | 정상 상태는 절제하고 비정상·조치 상태가 눈에 띄어야 함 | 운행·연결·정상 배터리 중립화, Yellow·Red를 예외와 조치 큐에 집중 |
| [Behance Enterprise Design System](https://www.behance.net/gallery/243340343/Enterprise-Design-System-SaaS-Platform-Case-Study), [Fleet Dashboard](https://www.behance.net/gallery/77060443/Automated-Fleet-Dashboard), [Mobile robots console](https://www.behance.net/gallery/130464827/Mobile-robots-console-fleet-management-UI-UX-design) | 강한 섹션 위계, 실제 제품 장면, 컴포넌트 근거를 함께 제시 | 장식형 Hero 제거, System Summary를 실제 토큰·컴포넌트·상세 링크 중심으로 구성 |
| [Pinterest dashboard case-study reference](https://in.pinterest.com/pin/dashboard-design-case-study-on-behance--384494886949943745/) | 빠른 스캔을 위한 카드 리듬과 명료한 인덱스 | 요약의 편집형 카드 리듬과 33개 목적지 인덱스에만 참고 |

Behance와 Pinterest는 표현·리듬 참고 자료다. 의미, 접근성, 운영 안전 규칙은 Apple·Google·Meta·W3C·ISA 같은 규범/플랫폼 자료보다 우선하지 않는다.

## 3. 감사한 33개 Parkie 목적지

- 시작하기 4개: Overview, System Summary, Principles, Change log
- Foundations 6개: Colors, Typography, Spacing, Radius, Elevation, Iconography
- Actions & Input 5개: Button, Segmented, Selection controls, Input, Site Select
- Status & Feedback 4개: Badge, Status Label, Alert, Alert Feed Item
- Navigation & Structure 6개: Top Bar, Tabs, Breadcrumb, Card, Table, Avatar
- Robot Operations 3개: Robot Status, Robot Card, Media & Emergency
- Overlays & Patterns 2개: Modal, Alert Center
- Templates 2개: Control App Shell, Control Dashboard
- Resources 1개: Brand assets

모든 목적지는 동일한 4개 문서 계약을 갖는다.

1. 사용 기준
2. 동작·구조
3. 접근성
4. 구현 규칙

## 4. 주차로봇 관제 시나리오

| 시간 목표 | 관제자가 답해야 하는 질문 | 화면 근거 |
| --- | --- | --- |
| 3초 | 데이터가 최신인가, P1이 있는가, 어떤 로봇이 멈췄는가 | 데이터 신선도, 조치 필요/오프라인 지표, P1 큐 |
| 30초 | 동작·통신·배터리 중 어느 축이 원인인가 | 7열 Fleet 표, 독립 상태축, 원인·영향·권장 조치 |
| 2분 | 안전하게 확인·복구 흐름을 시작할 수 있는가 | 로봇 상세, 충전 경로, 비상 확인 모달, 초점 복귀 |

이 시간은 현재 정보구조의 설계 목표이자 전문가 휴리스틱 프록시다. 실제 운영자 과업 시간은 별도 사용자 테스트에서 측정해야 한다.

## 5. 품질 게이트와 자동 증거

| 게이트 | 자동 증거 | 통과 기준 |
| --- | --- | --- |
| 정보 구조 | 33개 deep link, 현재 위치, history, KO/EN 검색 | 33/33 |
| 문서 깊이 | 목적지별 4개 고유 계약 | 33 × 4 |
| 접근성 | axe-core로 33개 전체 렌더 검사 | 위반 0 |
| DOM 무결성 | 33개 화면 duplicate ID 검사 | 중복 0 |
| 반응형 | 1440·900·390px document/main overflow | 페이지 overflow 0 |
| 타입 | Parkie HTML/CSS의 8–11px 리터럴 탐지 | 최소 12px |
| 아이콘 | 30개 출처, 조작 아이콘 6상태, 의미 아이콘 별도 축 | 30개 + 도메인 상태 22개 |
| 색 의미 | 선택·정보·운행·충전 토큰 비교 | 네 축 분리 |
| CCTV | Live·Stale·Reconnecting·Paused·Offline·Error | 6상태 + 4채널 반응형 |
| 키보드 | Tabs, role radio, combobox, Alert Center, 비상 모달 | roving focus, Escape, focus trap/return |
| 운영 밀도 | DOM에 100 robot rows, 200 alarm rows 합성 | 1초 이내 생성, 로컬 스크롤, 페이지 overflow 0 |
| 자산 | 런타임·CSS·아이콘·MS 번들·브랜드 SVG | 필수 자산 HTTP 200 |
| 토큰 | 모든 `var(--parkie-*)` 참조와 정의 비교 | 미정의 0 |
| 테마 | 소스·렌더 계약 | dark 고정, 토글 숨김 |
| 토큰 패리티 | 신규 토큰 root/dark 선언 수 + mutation | 2개 선언, 한쪽 삭제를 검출 |

명령:

```sh
npm run test:audit
npm run test:ui
npm test
```

## 6. 반복 검증 방식

검증은 횟수 채우기가 아니라 결함 소진 방식으로 수행한다.

1. 구조 루프: 33개 문서·랜드마크·제목·관계·중복 ID를 검사한다.
2. 의미 루프: Brand·interaction·operation·severity 축이 섞였는지 검사한다.
3. 제품 루프: 데이터 신선도, 로봇 상태축, CCTV 복구, P1 원인/영향/조치를 검사한다.
4. 스트레스 루프: 100 robots, 200 alarms, 4 CCTV와 좁은 문서 뷰를 검사한다.
5. 시각 루프: System Summary, Icon matrix, Dashboard, App Shell, Robot Status, Modal 캡처를 사람이 확인한다.
6. 최종 변경 이후 전체 스위트를 연속 3회 통과해야 릴리스한다. 변경이나 실패가 발생하면 카운트를 0으로 되돌린다.
7. 한쪽 테마의 신규 토큰을 제거한 가상 mutation이 테스트에서 실패로 검출되는지 확인한다.

## 7. 제거한 주요 결함

- Parkie 문서 일부만 검사하던 17개 범위를 전체 33개로 확장했다.
- Primary의 밝은 파란 배경 위 흰 글자를 6.42:1 다크 잉크로 교체했다.
- Hover/Pressed까지 파란 아이콘이던 규칙을 중립 피드백으로 바꾸고 Selected/On만 파란색으로 분리했다.
- 아이콘 매트릭스를 6개 상태가 줄바꿈되지 않는 실제 표 구조로 재설계했다.
- 정상 운행·연결·배터리를 Green에서 중립색으로 바꾸고 충전·주의·고장만 의미색으로 남겼다.
- CCTV에 누락됐던 Stale과 마지막 프레임 경과 시간을 추가했다.
- App Shell의 문자 기호를 Parkie/Adopted SVG로 교체했다.
- Dashboard에 데이터 신선도, 독립 상태축, 오프라인, P1/P2 원인·영향·권장 조치를 추가했다.
- Select·Modal·Alert Center·App Shell의 ARIA 관계, 입력 label, heading 계층, 중복 main을 수정했다.
- 비상 모달에 안전 기본 초점, Tab trap, Escape 종료와 트리거 초점 복귀를 추가했다.
- 12px 미만 Parkie 캡션을 RMS 타입 토큰으로 올렸다.

## 8. 릴리스 정책과 잔여 위험

- 최종 코드 변경 뒤 `npm test` 3회 연속 통과가 필요하다.
- 통과 커밋과 배포 커밋이 같아야 한다.
- GitHub Pages에서 해시, 주요 자산, 33개 경로와 대표 상호작용을 다시 확인한다.
- 자동검사는 실제 CCTV 지연, 로봇 명령 실패, 네트워크 손실, 스크린리더별 발음과 현장 조도까지 보증하지 않는다. 실제 장비 연동·운영자 과업·보조기술 수동검증은 제품 출시 전 별도 게이트다.

### 아이콘 규격에서 남겨둔 것 (2026-08-04 실측)

포커스 표본을 고치면서 아이콘 카탈로그를 Material 규격에 대조했다. 세 건을 적었고
**1번은 고쳤고, 2번은 다시 재는 과정에서 닫혔다**(근거가 틀렸다). 남은 것은 3번이며,
전달 도형을 다시 그리는 일이라 전달자와 확인이 필요하다.

**대조 기준을 그대로 적용하면 안 된다는 것이 2번의 교훈이다.** Material 수치는 Material
아이콘의 것이고, 이 카탈로그에는 Fluent 채택본이 섞여 있다. 다른 시스템의 잣대를 댈
때는 그 시스템이 왜 그 값을 골랐는지까지 봐야 한다.

**기준으로 삼은 Material 수치** — 트림 24×24dp, 라이브 영역 20×20dp, 여백 2dp,
키라인(정사각 18×18 · 원 ⌀20 · 세로 20×16 · 가로 16×20), 획 2dp, 24dp 심볼의 터치
타깃 48dp. Google의 포커스 링은 `--md-focus-ring-width: 3px`이되 **색은 불투명**이다.

#### 포커스 표본을 62% 알파로 되돌린 것 (2026-08-05)

**위의 "색은 불투명" 근거는 이 표본에 대해서는 철회한다.** `decb82d`가 3px 40%를 2px
불투명으로 바꾼 것은 대비 계산으로는 옳았지만(2.11:1 → 7.24:1) 실사용 판단이 빠져
있었다. 7.24:1짜리 시안 테두리는 링이 아니라 **불 켜진 테두리**로 읽힌다. 그때 문서에
"알파는 잘못된 다이얼"이라고 적었는데, 정확히는 **알파가 유일하게 남은 다이얼**이었다 —
면적은 SC 2.4.13이 잠가두었으므로.

바꾼 값은 `--parkie-focus-ring-icon: color-mix(in srgb, var(--parkie-color-brand-500) 62%,
transparent)`이고, 이 페이지 표면 위 합성 실측은 다음과 같다.

| 알파 | 합성색 | 대비 | |
|---|---|---|---|
| 1.00 | `rgb(0,170,255)` | 7.24:1 | 이전 값, 너무 밝다 |
| **0.62** | `rgb(7,113,166)` | **3.45:1** | 채택 |
| 0.58 | `rgb(8,107,157)` | 3.16:1 | |
| 0.55 | `rgb(9,102,150)` | 2.96:1 | 하한 아래 |
| 0.40 | `rgb(11,79,115)` | 2.11:1 | 전달 표본 |

**Google 사례가 이 건을 정하지 못하는 이유**도 함께 적어둔다. Material의 링은 밝은
표면 위에 얹히고, 여기는 `#131315`다. 어두운 배경에서 밝기는 곧 대비이므로 "덜 밝게"와
"대비 유지"가 같은 축에서 충돌한다. 그래서 답은 취향이 아니라 **하한을 찾는 계산**이었다.

게이트도 이에 맞춰 양쪽 괄호로 바꿨다(`tests/ui.spec.js`). 이전 게이트는 `alpha === 1`을
단정해서 "너무 밝다"는 결함을 **잡을 수 없는 방향으로** 잠가둔 상태였다. 지금은 알파가
1보다 작아야 하고(밝기 상한) 합성 대비가 3:1 이상이어야 한다(접근성 하한). 그리고 본문
수치를 하드코딩 대신 **실측값에서 읽어** 비교하므로, 토큰만 바꾸고 설명을 안 고치면
실패한다 — 직전 게이트가 `2.11`/`7.24`/`0.60` 리터럴을 고정했는데 그 셋 다 참인 채로
아래 값이 바뀔 수 있었던 것이 이 교훈이다.

1. **컨테이너 30px → 32px. 완료.** `.pk-icon-state`의 `is-focus`·`is-pressed`·
   `is-selected`·칩이 30×30이라 24 글리프에 여백이 3px씩 붙었다. 3은 `--parkie-space-*`
   (4/8/12/16…)에 없는 값이다. 32에서 여백이 4px가 되어 격자에 맞고, Fluent·Carbon·Ant가
   24px 아이콘에 주는 컨테이너와 같아진다.

   **아이콘 그리드 자체는 처음부터 24×24로 맞았다** — 30은 글리프가 아니라 판이었다.
   글리프는 그대로 24px이고 판만 움직였다.

   포커스 링의 WCAG 면적 하한도 함께 움직였는데, 게이트가 판 크기를 빌드에서 읽어
   계산하므로 손댈 것이 없었다 — 32판에서 2px 링은 272px²로 하한 272px²에 정확히 걸린다.
   30판일 때의 256/256과 같은 관계다.

   Material은 24dp 심볼에 터치 타깃 48dp를 요구하지만 이것들은 **컨트롤이 아니라 문서
   표본**이다. 제품의 실제 아이콘 버튼(`.pk-icon-button`, 현재 24×24)은 별개 사안이고
   아직 열려 있다.

2. **`Refresh` — 닫힘. 고칠 것이 없었다.** 처음에 "세로 21.5dp로 20dp 라이브 영역을
   초과하는 유일한 아이콘"이라 적었다. 다시 재보니 크기는 맞지만 **결론이 틀렸다.**

   `ArrowSync`는 Fluent가 **모든 사이즈에서 일관되게 20dp보다 크게** 그린 글리프다:

   | 변형 | 24 환산 | 여백 상/하 |
   |---|---|---|
   | **Size24 Regular (채택본)** | 17.50 × 21.50 | **1.25** |
   | Size20 Regular | 18.00 × 22.41 | 0.79 |
   | Size16 Regular | 18.00 × 22.50 | 0.75 |
   | Size24 Filled | 18.00 × 23.00 | 0.50 |

   **넷 중 지금 채택한 것이 가장 잘 들어맞는다.** 원저자의 실수가 아니라 의도이고,
   바꾸면 `Adopted`(검증된 원본 채택) 표시와 어긋난다. 잘림도 없다 —
   `.pk-icon-state svg`가 `overflow: visible`이다.

   맞춘다면 0.930배 축소인데, 폭이 16.28로 **`Play`(16.86)보다 좁아져 옆줄보다 작아
   보이고** 획이 7% 얇아진다. 숫자 하나를 맞추려고 보이는 것을 나쁘게 만드는 교환이다.

   **원인은 잣대를 잘못 댄 것이다.** Material의 20dp 라이브 영역 규칙을 Fluent 채택
   아이콘에 그대로 들이댔다. Material 자신도 원(⌀20)을 정사각(18×18)보다 크게 잡는데,
   얇은 링은 같은 크기로 보이려면 더 커야 하기 때문이다. `Refresh`가 그 경우다.

3. **4개가 광학적으로 작다.** 처음에 7개라고 적었으나 다시 재서 셋이 빠졌다(아래 정정).

   | 아이콘 | 처음 적은 값 | 실제 잉크 | 키라인 | 차이 |
   |---|---|---|---|---|
   | `Charger` | 11×11 | **13×13** | 정사각 18×18 | −5 |
   | `Robot` | 14×13 | **16×15** | 정사각 18×18 | −2 / −3 |
   | `Obstacle` | 16×10 | **18×12** | 가로 20×16 | −2 / −4 |
   | `Vehicle transport` | 16×12 | **18×14** | 가로 20×16 | −2 / −2 |

   **정정 (2026-08-04 재측정).** 세 가지가 틀렸다.

   - **`More`와 `Filter`는 `MS Adopted`다** (`index.html`의 `iconRow`). 다시 그리면
     2번에서 철회한 실수를 그대로 반복하고 `Adopted`(검증된 원본 채택) 표시와 어긋난다.
     특히 `More`(15.5×3.5)는 가로로 놓인 점 세 개라 어떤 키라인도 적용 대상이 아니다
   - **`Route`는 잉크가 정확히 18.00×18.00**으로 Material 정사각 키라인에 이미 맞는다
   - 처음 적은 숫자는 전부 `getBBox()` 값인데 **이 아이콘들은 `stroke-width="2"` ·
     `fill="none"`이라 그 값에 획이 빠져 있다.** `getBBox()`는 기하 경계만 돌려주고
     획 두께를 포함하지 않는다. 실제 잉크는 각 변에서 +2다

   **`getBBox()`로 선 아이콘의 크기를 재지 마라.** 채움 아이콘(MS 채택본)에서는 잉크와
   기하가 같지만 선 아이콘에서는 다르다. 두 종류가 한 카탈로그에 섞여 있으므로, 같은
   자로 재려면 **렌더한 뒤 실제로 칠해진 픽셀**을 재야 한다 — 40배로 래스터라이즈해
   알파가 있는 픽셀의 경계 상자를 구하면 획·캡·조인이 전부 포함된다.

**측정 방법 — 여기서 한 번 틀렸으니 그대로 따라 할 것.** 페이지를 렌더한 뒤
**`<svg>` 루트에서** `getBBox()`를 부른다. 자식들의 transform이 적용된 합집합이 나온다.

처음에는 각 `<path>`에서 `getBBox()`를 불렀고, 그게 틀렸다. **`getBBox()`는 그 요소
자신의 `transform`을 적용하지 않는다.** MS 채택 아이콘은 전부 path에
`transform="matrix(1 0 0 1 3.25 1.25)"` 같은 이동이 붙어 있어서, 크기(w×h)는 맞게
나오지만 **원점과 여백이 전부 틀리게** 나온다. 실제로 `Refresh`의 여백을 1.25가 아니라
0.00으로 읽었고, 그 값으로 결론을 냈다.

**여백이나 정렬을 판단할 때는 반드시 루트에서 재야 한다.** transform은 크기(w×h)를
바꾸지 않으므로 크기만 쓰는 항목은 이 실수의 영향을 받지 않는다.

**다만 3번이 두 번째 실수에 걸렸다** — transform이 아니라 **획**이다. 두 실수는 별개이며,
루트에서 부르는 것으로는 두 번째가 해결되지 않는다. `getBBox()`는 어디서 부르든 획을
포함하지 않는다. 선 아이콘이 섞인 카탈로그에서는 3번의 정정에 적은 대로 **칠해진 픽셀**을
재야 한다.

### 통신 상태를 형태 축으로 옮긴 것 (2026-08-04)

같은 로봇 상태 어휘가 세 곳에서 따로 그려지고 있었다 — 아이콘 카탈로그의 `로봇 상태`
그룹(4상태), 도메인 그룹 `배터리·통신`(5상태), 제품 배지 `.pk-status--*`(5상태). 라벨도
글리프도 색 경로도 달랐고, **재연결 중이 한쪽은 wifi 마크, 다른 쪽은 sync 화살표**였다.

가장 큰 결함은 **연결 끊김과 오프라인이 같은 글리프를 쓰고 색만 달랐던 것**이다. 그룹
설명이 *"형태와 색상 두 축으로 구분합니다"*라고 적혀 있었으므로 **빌드가 자기 문서와
어긋나 있었고**, 색만으로 정보를 전달해 WCAG 1.4.1에도 걸렸다.

- 두 문서 표면을 **한 배열(`robotStateVocabulary`)에서 파생**시켜 어긋날 수 없게 했다
- 오프라인용 글리프 `ConnectionOffline`을 신설했다. 끊김은 사선이 있고 오프라인은 없다
- **배터리의 규칙을 통신에 적용했다** — 형태가 단계를, 색은 심각도만. 약함의 노랑과
  재연결의 하늘색을 중립으로 내렸다. 약함은 호가 3→2개로 이미 줄어 형태가 세기를 말하고,
  재연결은 회전 모션과 원형 화살표가 말한다
- `--parkie-connection-offline`은 **토큰만 있고 소비자가 없었다.** `robot-status.css`도
  `--parkie-status-info`·`--parkie-status-warning`을 직접 읽어 연결 토큰을 우회하고
  있었으므로, 토큰만 옮겼다면 제품 배지는 파랑·노랑 그대로 남았을 것이다

**5상태로 늘리자 카탈로그 행이 깨졌다.** `grid-template-columns`가 4열로 고정돼 있어
다섯 번째가 다음 줄 라벨 칸에 들어갔다. `repeat()`는 count에 커스텀 속성을 못 받으므로
개수를 클래스로 넘기고, **렌더된 열 수가 상태 수와 같은지를 게이트가 잰다** — 여섯 번째가
생기면 조용히 줄바꿈되는 대신 테스트가 실패한다.

### 액션 채움을 한 단 내려 흰 글씨를 쓴 것 (2026-08-05)

제품 요구는 "파란 버튼 안 글씨는 모두 흰색"이었다. 흰색은 `#00AAFF` 위에서 2.56:1로
AA(4.5:1)는 물론 큰 글씨 기준(3:1)에도 못 미치므로, **글씨가 아니라 채움을 내렸다.**
어두운 잉크가 틀렸던 것은 아니다 — 6.42:1로 옳은 값이었고 다만 제품이 원하는 모양이
아니었다. `#00AAFF`는 브랜드 색으로 남는다(아이콘·Selected·테두리·Subtle·Outline).

| 상태 | 토큰 | hex | 흰 글씨 |
|---|---|---|---|
| Rest | `brand-700` | `#007CBD` | 4.55:1 |
| Hover | `brand-800` | `#08638F` | 6.58:1 |
| Pressed | `brand-900` | `#0D4F70` | 8.86:1 |

**새 색을 만들지 않았다.** 기존 램프의 세 단을 그대로 쓴다 — Danger가
`--parkie-color-red-700/800`을 재사용하는 것과 같은 구조다.

**램프가 단조 감광하는 것은 취향이 아니다.** 흰 글씨가 4.5:1을 넘는 가장 밝은 파랑이
대략 `#007CBA`이고 rest가 이미 그 자리다. hover를 밝히면 4.35:1로 떨어지는데, 대비 표의
판정은 `r >= 4.5`만 통과로 보므로 `AA Large`도 `is-fail`을 단다. 위로 올릴 여유가 없다.
결과적으로 Primary가 Danger와 같은 모양(5.08 → 6.91 → 8.94)이 되었다.

**이 변경이 잡아낸 결함 1건 — 채움 토큰을 잉크로 읽고 있었다.**
`.pk-robot-command--primary`가 `color: var(--parkie-action-primary-bg)`였다. 채움이
`#00AAFF`인 동안은 카드 위에서 7.24:1로 **우연히** 통과했고, 채움이 흰 글씨를 받으려고
한 단 내려간 순간 4.08:1로 AA 아래로 떨어졌다. axe 33라우트 스윕이 이것을 잡았다.
고친 값은 `--parkie-brand-text`(9.46:1)이며, **이것이 잉크로 쓰도록 만들어진 토큰**이다.

교훈은 §12의 것과 같은 종류다. 게이트가 잡았다는 것이 요점이 아니라, **이름이 용도를
말하는 토큰을 용도와 다르게 쓰면 값이 우연히 맞는 동안만 조용하다**는 것이다.
`-bg`로 끝나는 토큰이 `color:`에 앉아 있으면 그 자체가 지적 대상이다.

## 9. 다제품 구조와 Goalie 뼈대

1~8절은 **Parkie 단일 제품**에 대한 감사 기록이며 그 범위 안에서 유효하다.
이후 사이트는 두 제품을 서빙하는 구조로 바뀌었다.

### 구조

- 크롬(상단바·사이드바)은 `--guide-*` **제품 중립 토큰층**을 쓴다. 제품이 늘어도 크롬을 다시 만들지 않는다.
- 제품 탭은 상단바 안 인라인 탭 한 줄이다. 크롬 높이는 넓은 폭 64px, 좁은 폭 92px로 고정된다.
- 활성 제품은 **밑줄 하나**로 표시한다. 배경·테두리·색을 겹치는 3중 표시는 눌린 버튼으로 읽히므로 쓰지 않는다.
- 좁은 폭 배치는 `flex-wrap`이 아니라 **명시적 grid**다. 줄바꿈은 픽셀 산술에서 창발하므로 한 폭에서 맞고 다음 폭에서 깨졌다.
- 제품 토큰은 자기 스코프 안에만 존재하고 `:root` 기본값이 없다. 래퍼 누락 시 눈에 보이게 실패한다.

### Goalie의 감사 상태

**Goalie는 아직 감사 대상이 아니다.** 승인된 디자인 시스템이 없기 때문이다.
13개 페이지 중 `브랜드 자산`(인수 계약)만 실내용이고 나머지 12개는 전달
대기 상태의 빈 슬롯이다. 자료를 전달받아 채운 페이지부터 순차로 이 문서의
감사 범위에 들어온다.

경로·접근성·구조 검사는 이미 두 제품 전 경로에 적용된다 — `h1` 존재, `main`
단일, 중복 ID 0, axe 위반 0.

### 추가된 품질 게이트

- **패리티 게이트 앵커링** — 이전 구현은 `indexOf(':root')`로 스코프를 찾아 전체 선택자에 우연히 접두 일치했고, 선언 수를 **파일 전체**에서 셌다. 두 가정 모두 스코프 밖 선언이 생기는 순간 깨진다. 정확한 선택자로 앵커링하고 스코프별로 센다.
- **Goalie 스코프 게이트** — `:root` 기본값 부재, 참조된 `--goalie-*` 전부 선언됨(93개 참조 / 135개 선언), 고대비 오버라이드가 실존 토큰만 건드림.
- **고대비 렌더 검증** — 소스 텍스트가 아니라 `forcedColors: 'active'` 에뮬레이션에서 실제 계산값을 읽는다. 텍스트만 검사하던 초기 버전은 규칙이 아무 일도 하지 않는 동안 통과했다(`forced-colors` 블록이 `.guide-nav-item__soon` 정의보다 앞에 있어 동일 특이도에서 밀렸다). 렌더 검증만이 이 결함을 잡는다.

### 이 단계에서 제거한 결함

- 생성된 Goalie 콘텐츠 13페이지가 실제 제품명으로 공개돼 있던 상태를 뼈대로 되돌렸다.
- `soon` 배지가 라이트 크롬에서 4.45:1(12px 라벨, AA 미달)이었다. Goalie에 `soon` 항목이 없어 한 번도 렌더되지 않은 조합이다. 7.79:1 / 6.20:1로 교체했다.
- 제품 탭이 줄바꿈으로 자기 줄에 놓일 때 높이가 16px로 붕괴했다(WCAG 2.2 최소 타깃 24px 미달). 36px 하한을 넣었다.
- 390px에서 크롬이 151px, 320px에서 112px로 폭마다 다르게 깨졌다. 모든 좁은 폭에서 92px로 고정했다.
- 제품명이 상단탭·사이드바·본문 eyebrow에 3중 반복됐다. eyebrow는 현재 IA 그룹을 보여준다.
- Parkie 토큰과 크롬에 `forced-colors` 대응이 각 0건이었다.

## 10. 제품 레지스트리와 세 번째 탭

9절은 제품 2개 구조를 다뤘다. 세 번째 탭 **CPMS UI**(PM 전용 프로젝트 관리 툴, 라이트 고정)를
추가하면서 제품 분기를 레지스트리로 걷어냈다. 자세한 구조는 `PARKIE_MERGE_PLAN.md` 14절에 있다.

### 추가 시점의 감사 상태

CPMS 탭을 처음 추가한 시점에는 Goalie와 마찬가지로 감사 대상이 아니었다.
13개 페이지 중 `브랜드 자산`(인수 계약)만 실내용이고 나머지 12개는 전달 대기 슬롯이다.
경로·접근성·구조·고대비 검사는 세 제품 전 경로에 이미 적용된다.

### 추가된 게이트

- **레지스트리 고정** — 등록된 제품 목록·순서, 제품별 고정 테마, 탭 라벨과 경로가 레지스트리와 일치하는지. 나머지 검사는 레지스트리에서 파생되므로, 이 고정이 없으면 제품이 사라져도 통과한다
- **첫 페인트 계약을 전 제품으로** — 콜드 딥링크에서 각 제품이 다른 제품의 테마를 한 프레임도 보이지 않는지. 이전에는 Goalie만 검사했다
- **제품 토큰 스코프 게이트를 매개변수화** — `:root` 부재, 참조 전부 선언, 고대비 오버라이드가 실존 토큰만, `--product-*` 별칭 발행. 제품별 복사가 아니라 표의 한 행이다
- **고대비 렌더 검증을 세 제품으로** 확장

### 이 단계에서 제거한 결함

- 제품 루트를 칠하는 규칙이 `[data-product-root][data-system="…"]`로 **제품마다 하드코딩**돼 있었다. CPMS를 붙였을 때 그것을 빠뜨려 배경이 투명하게 렌더됐고 토큰 격리 테스트가 잡았다. 각 토큰 파일이 `--product-*`를 발행하고 셸에는 규칙 하나만 두는 구조로 바꿨다
- `guide-content-frame--goalie`는 **어디에도 정의되지 않은 죽은 클래스명**이었다. `--skeleton`으로 바꾸고 실제 규칙(최대 폭)을 붙였다
- `components/goalie.css`는 클래스 99개를 정의하는데 뼈대가 쓰는 것은 16개였다. 제품마다 복사했다면 사문화 83개가 제품 수만큼 늘어난다. 쓰이는 16개를 `--guide-*` 기반 공유 스타일로 분리했다

### 변이 검증

새 게이트는 실패해야 할 때 실패하는지 주입해 확인했다 — 레지스트리에서 CPMS 제거,
CPMS 고정 테마 변경, CPMS 토큰에 `:root` 기본값 주입, 미선언 `--cpms-*` 참조,
`--product-bg` 별칭 제거. **5건 전부 검출**되고 복원 후 전부 통과한다.

### CPMS 1차 authored 시스템으로의 전환

CPMS는 이후 `skeleton: false`로 전환했고 `CPMSPages.dc.html`이 13개 전용
라우트를 렌더한다. 기준은 읽기 전용 소스 `aws-cpms@32ff10f`,
합성 데이터 시각 참고 `main@f321694`다. 운영 UI를 하나의 완성된
라이브러리로 오해하지 않도록 다음 상태를 항상 분리한다.

- `Astryx adopted` — Theme 경계, SideNav, App Store의 직접 컴포넌트 사용
- `CPMS custom` — 주요 PM·일정·데이터·현장 업무 화면
- `Legacy supported` — OP/FM의 명시적 pre-Astryx 경계
- `Candidate` — 접근성·의미 토큰·상태 계약을 포함한 이전 권장안

기존 자리표시자 인디고 토큰은 현재 HL Sky Blue `#00B4ED`,
Deep Blue `#007AA5`, Navy `#002B68`과 실제 중립·상태 근거로 교체했다.
Sky는 선택·액센트, Navy는 흰 텍스트가 필요한 주요 액션에 사용한다.

검증은 Goalie 스켈레톤 라우트와 CPMS authored 라우트를 분리한다.
CPMS 전 라우트에 대해 딕링크·새로고침·KO/EN·axe·제품 토큰 격리·
320–1440px 리플로와 출처 commit 고정을 검사한다.

## 11. 릴리스 검증 절차

검증이 라운드마다 새로운 것을 찾아내는 상태를 끝내기 위해 절차를 고정한다.
이전에는 검증이 **탐색적**이었다 — 새 각도를 잡을 때마다 뭔가 나오고, 안 잡으면 안 나왔다.
그래서 "언제 끝났는지"를 말할 수 없었다. 아래 넷을 실행하면 끝난 것이다.

```
node tests/server.js &                      # 개발 서버

npm test                                    # 1. 3회 연속 통과
npm test
npm test

npx playwright test tests/route-sweep.spec.js   # 2. 88경로 스윕 (npm test에 포함)

node tests/tools/route-baseline.js before   # 3. 변경 전 기록
#   … 변경 …
node tests/tools/route-baseline.js after
node tests/tools/route-baseline.js --diff before after
```

4. 배포 후 라이브에서 레지스트리·크롬 높이·뼈대·고대비를 확인한다.
   **배포 커밋 = 통과 커밋**이어야 한다.

### 각 단계가 무엇을 잡나

| 단계 | 잡는 것 |
|---|---|
| `npm test` 3회 | 단정된 계약 전부. 3회는 비결정성 배제 |
| 88경로 스윕 | 표본에서 빠진 페이지의 런타임 에러·404·중복 id·빈 제목·오버플로·제품 프레임 미채색 |
| 행동 기준선 | 리팩터가 **의도하지 않은 것을 바꾸지 않았는지.** 단정이 아니라 전후 대조다 |
| 라이브 확인 | 배포 파이프라인이 통과한 것과 같은 것을 서빙하는지 |

행동 기준선은 스펙이 아니다. 스펙은 "무엇이 참이어야 하는가"를 말하고, 기준선은
"무엇이 참이었는가"를 기록해 의도치 않은 변화를 차이로 드러낸다. 제품 레지스트리
리팩터가 기존 75경로를 건드리지 않았음을 이것으로 증명했다.

## 12. 변이 검증 — 게이트가 실제로 잡는지

**통과하는 테스트는 그 자체로 증거가 아니다.** 지키겠다고 주장하는 것을 망가뜨렸을 때
실패해야 비로소 테스트다. 게이트마다 그 대상을 파괴해 검출 여부를 확인했다.

약 35건의 변이를 주입했고, 그중 상당수는 **변이 자체가 빗나가** 재조준이 필요했다
(`display: none`은 `toHaveCount`를 바꾸지 않고, `alt=""`는 유효한 장식 이미지이며,
`hint-placeholder-count`는 에디터 힌트일 뿐 데이터가 아니다). 재조준 후 검출됐다면
게이트는 건강한 것이다.

### 이 방식으로 드러난 무력한 단정 3건

| 게이트 | 문제 | 조치 |
|---|---|---|
| 탭 포커스 링 | 아웃라인 **폭만** 재서, 우리 `:focus-visible` 규칙을 지워도 브라우저 기본 링 때문에 통과했다 | 해석된 `--guide-accent`와 **색**을 비교. UA 기본값은 결코 그 색이 아니다 |
| alert-center 로빙 포커스 | 이름은 "roving"인데 `tabindex`를 전혀 보지 않았다. 두 탭이 모두 `tabindex="0"`이어도 통과 | 선택된 탭만 탭 순서에 있고 선택을 따라 이동하는지 단정 |
| 플릿 로컬 스크롤 | `scrollWidth >= clientWidth` — **정의상 항상 참**이라 실패할 수 없었다 | 엄격한 `>`와 `overflow-x`가 스크롤 값인지 확인 |

### 범위가 좁았던 게이트 1건

`interaction, operation and severity colors remain independent`가 심각도 계열
(`success`·`warning`·`danger`·`emergency`)을 **하나도** 검사하지 않았다.
`--parkie-success`를 브랜드 블루로 바꿔도 통과했다. 의미 토큰 전체가 상호작용 색과
달라야 하고, 심각도끼리도 서로 달라야 함을 단정하도록 넓혔다.

### 검증 대상이 아닌 것

`tests/visual.spec.js`는 **캡처 하네스**다. `test-results/review/`에 스크린샷을 남길 뿐
기준 이미지와 비교하지 않는다. 사람이 볼 자료를 만드는 도구이므로 단정이 아니고,
변이 검증 대상도 아니다. 시각 회귀를 자동으로 잡아 주지 **않는다**는 뜻이다.

### 앞으로

커버리지 행렬이 채워졌고 게이트가 변이 검증됐으므로, 이후 발견되는 것은 커버리지
구멍이 아니라 **신규 결함**으로 다룬다. 게이트를 새로 추가할 때는 그 게이트가 실패하는
변이를 함께 확인하고, 확인하지 못하면 그 게이트는 아직 테스트가 아니다.
