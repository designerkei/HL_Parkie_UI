# Goalie UI — 인수인계

최종 갱신 2026-08-04. 다음 작업자가 이 문서만 읽고 이어갈 수 있게 정리한다.

## 0. 다른 PC에서 다시 시작하기

인터넷이 되는 곳에서 아래 순서로 그대로 복구된다. 저장소 밖 의존성은 **Chrome 하나뿐이다.**

```
git clone https://github.com/designerkei/HL_Parkie_UI.git
cd HL_Parkie_UI
npm ci                          # package-lock.json이 커밋돼 있다
npx playwright install chrome   # 아래 주의 참고
node tests/server.js &
npm test                        # 87개 통과하면 환경 복구 완료
```

- **Chrome이 있어야 한다.** `playwright.config.js`가 `channel: 'chrome'`이라 번들 chromium이
  아니라 실제 Google Chrome을 쓴다. 없으면 87개가 전부 실행조차 안 된다
- 검증된 기준선은 `node v22.23.1` · `npm 10.9.8`. **`node v26.4.0` · `npm 11.17.0`에서도
  87개가 전부 통과한다** (8월 4일 확인). 다만 **스위트가 50초가 아니라 3분대로 느리다.**
  6회 실행이 2.8 · 3.3 · 3.5 · 3.2 · 3.2 · 3.2분으로 전부 3분 안팎이었으니 **최초 실행의
  찬 캐시 탓이 아니다** — 처음엔 그렇게 의심했는데 2회차부터도 같았다. 시간의 대부분은
  `design-system-audit.spec.js`의 axe 33라우트 검사 하나(2.2분)다.

  원인을 아직 못 짚었다. node 22 기준선에서 다시 재보는 것이 가장 빠른 가름이다.

  **`timeout: 90_000`과 헷갈리지 말 것.** 긴 테스트들은 본문에서 각자
  `test.setTimeout()`을 선언한다 (axe 33라우트는 180초, route-sweep과 goalie-audit은
  600초). 전역 90초가 실제로 지키는 것은 **그 선언이 닿지 않는 fixture 설정 구간**이고,
  간헐 실패가 나던 곳이 정확히 거기다 (§1 참고). 느려진 환경이 위협하는 것도 본문이
  아니라 그 구간이다.

  다만 본문 여유도 줄었다 — axe 검사가 180초 예산 중 132초를 쓴다. 예전 속도라면
  한참 밑이었을 값이다. **`flaky` 줄이 늘기 시작하면 이 항목을 먼저 의심할 것.**
- gitignore 대상: `node_modules/` · `test-results/` · `playwright-report/` · `.route-baseline/`.
  특히 **`.route-baseline/`는 따라오지 않으므로** §4의 before/after diff는 한 PC 안에서
  한 세션에 끝내야 한다. 다른 PC에서 뜬 before와는 비교할 수 없다
- 라이브 확인: <https://designerkei.github.io/HL_Parkie_UI/> (`.nojekyll`, `main` 루트를 그대로 서빙)
- 상세 계획 원본 `~/.claude/plans/adaptive-twirling-goose.md`는 저장소 밖이라 **동기화되지 않는다.**
  필요한 내용은 이 문서 안에 옮겨져 있으니 그 파일을 찾지 말 것

## 1. 지금 상태

| | |
|---|---|
| 브랜치 | `main` (배포 대상. Pages가 루트를 그대로 서빙 — 커밋 = 배포) |
| 마지막 검증 커밋 | `1cec34f` — `npm test` **87개 3회 연속 통과** |
| 미검증 커밋 | 이 문서를 갱신한 커밋. 문서 전용이라 페이지에 영향 없음 |
| origin 동기화 | `main` = `origin/main` — 푸시·배포까지 끝난 상태 |
| Goalie 페이지 | 15개, 전부 `impl: true`로 공개 |
| 미착수 백로그 | §3 참고. **B는 전부 닫혔다.** 남은 것은 A-1~A-3(설계 결정)과 wip 브랜치 |

**작성 시점의 HEAD를 그대로 적지 말 것.** 8월 2일에 이 문서가 `c3a4e0b` 하나만 미검증으로
적어둔 사이 커밋 3개가 더 쌓여 검증 대상에서 빠졌다. 해시가 아니라
`git log <마지막 검증>..HEAD` **범위**로 확인한다.

```
node tests/server.js &     # 이미 4173을 쓰고 있으면 그대로 재사용 (playwright reuseExistingServer)
npm test                   # 3회 연속 통과가 릴리스 기준
```

### 커밋 신원 — 이 PC에서 한 번 걸렸다

**커밋 신원을 먼저 확인하라.** 이 PC의 전역 git 설정이 회사 도메인 주소였고, 그대로
커밋했으면 공개 저장소에 남을 뻔했다. 저장소 로컬 설정으로
`designerkei <239721783+designerkei@users.noreply.github.com>`을 고정해 두었다
(`git config --local`). 새 클론에서는 다시 해야 한다 — §6 참고.

(주소 자체는 여기 적지 않는다. 공개되는 파일이고, 적는 순간 막으려던 일을 대신 하게 된다.)

### 8월 3일에 있었던 일 — 롤백 1건

다른 PC에서 올라온 `6c833da` · `6435458`(아이콘 페이지 재작성)이 `main`을 망가뜨렸다.
`index.html`이 5,131 → 2,815줄로 잘려 **사이드바 목적지가 0개**가 됐고, 렌더되지 않은
`{{ preview.dataUrl }}`이 URL로 요청돼 404가 났다. `npm test`가 400초를 넘겨 끝나지 않았다.

`66a65ea`로 두 커밋을 revert했다. 이미 배포된 커밋이고 여러 작업자가 쓰는 저장소라
`reset --hard` + 강제 푸시 대신 revert를 썼다. **작업은 버리지 않았다** —
`wip/iconography-rewrite-2026-08-03`에 원격 보존돼 있다. §8 참고.

### 그 뒤 Parkie 아이콘 쪽에서 끝낸 것

| 커밋 | 내용 |
|---|---|
| `6604c3e` | 아이콘 도형 SVG 일괄 내려받기 (ZIP 38개). 자체 STORE ZIP 라이터, 의존성 0 |
| `27bdf4f` → `ce45979` | 상태별 낱개 152파일 → **상태 시트 SVG 한 장**으로 교체 |
| `b5f6f50` | `hover`·`focus`·`pressed` 토큰이 셋 다 0.95였던 것을 0.85 / 0.95 / 1 로 분리 |
| `c9595b8` | 요약 칩이 행 수(30)를 아이콘 수라고 말하던 것을 실제 도형 수(38)로 |
| `d427764` · `8077988` | 획 두께 1.8/1.9/2/2.2 네 종 → **2px 11개 + 1.8px 2개** |

- **획 척도는 테스트에 정책으로 고정돼 있다.** `1.8`과 `2` 외의 값이 들어오면
  `tests/design-system-audit.spec.js`가 실패한다. 넓히려면 테스트를 의도적으로 고쳐야 한다.
  예외 2개는 측정 근거가 있다 — ParkingBay는 2px에서 속공간이 가독 하한 4px²로 떨어지고,
  ManualControl은 틈이 아예 봉해진다 (24px 렌더에서 flood fill로 측정)
- **요약 칩은 이제 카탈로그에서 파생된다.** 문자열로 적어두면 또 어긋난다 (두 번 어긋났다)

### 8월 4일에 Goalie 쪽에서 끝낸 것

| 커밋 | 내용 |
|---|---|
| `92db176` | **B-1.** 페이지가 "Pressed는 아직 기준 미달"이라 적었는데 이미 통과하고 있었다 |
| `d23a23f` · `f6308d5` | 테스트 간헐 실패 — 전역 타임아웃 90초, 재시도 1회 |
| `a8a7c3b` | **C.** 감사 도구 범위 정비 + `tests/goalie-audit.spec.js` 게이트 편입 |
| `cc83a21` | C가 낸 목록 처리 — 20건 → 5건 |
| `f6308d5` | 남은 5건(시안 스펙값)을 B-1 방식으로 문서화 |

**B-1은 손댈 게 없었다.** 핸드오프가 "채움을 어둡게 해야 풀린다"고 적었지만,
`components/goalie.css`가 그 상태에만 잉크를 순수 검정으로 내리고 있어 이미 4.67:1이었다.
원래 근거가 잉크 후보를 흰색과 `#1C2126` 둘만 재고 "잉크로는 못 푼다"고 결론냈던 것이다.

### 8월 4일 두 번째 세션 — B 백로그를 전부 닫았다

| 커밋 | 내용 |
|---|---|
| `31e4eb9` | **B-4.** 근거가 사라졌다 — 문서가 잉크가 아니라 채움색을 재고 있었다 |
| `33cbbaf` | `route-baseline.js`가 "every route"라면서 90개 중 88개만 보고 있었다 |
| `63fe7c3` | **B-2.** `opacity: 0.3` → 상태별 전용 토큰 + 게이트 |
| `e1b4390` | **B-5.** 전달 표면이 이미 레이어였다 — 알파로 되돌림 + 게이트 |
| `1cec34f` | **B-3.** 채움은 그대로 두고 눌리는 상태에 경계 추가 + 게이트 |

게이트 셋 다 §5대로 **변이로 검증**했다 (총 7종, 전부 재시도까지 두 번 실패). 그중
하나는 선언 순서 변이였다 — B-3의 새 규칙은 기존 규칙과 **명시도가 같아서 뒤에 오는
것만으로 이긴다.** 위로 옮기면 조용히 예전 동작으로 돌아가므로, 그 상황을 실제로
만들어서 게이트가 잡는지 확인했다.

### 진행 중에 발견한 것 — 아직 안 고친 게 하나 있다

**영어 번역 게이트가 15페이지 중 2개만 본다.** `multi-system.spec.js`의
"authored Goalie copy is fully translated in English"가 `overview`와 `brand`만 돈다.
실제로 재보니 **버튼 페이지는 EN 모드에서 한글이 124자 남는다.** 이번에 추가한 문구는
아니다 (새 `data-spec-edge` 노트는 EN에서 한글 0자로 확인했다). 기존 미번역분이고,
게이트가 그 페이지를 안 봐서 드러나지 않았을 뿐이다.

고치려면 페이지별로 미번역 문구를 찾아 `L()`로 감싼 뒤 게이트의 페이지 목록을 넓혀야
한다. **목록만 먼저 넓히면 즉시 빨간불이 된다.** 범위가 커서 이번에는 손대지 않았다.

### 감사 도구를 믿어도 되는 이유 — 거짓 양성 3종을 다 잡았다

`tests/tools/goalie-audit.js` 헤더에 전부 적혀 있다. 요약하면:

1. **반투명 배경 미합성** → 흰 텍스트를 흰 배경에 대고 1.05:1 오보 (이전에 수정됨)
2. **경계 범위** → 약 290건이 `--goalie-border` 카드 구분선이었다. WCAG 1.4.11은 컴포넌트·
   상태를 식별하는 경계에만 적용된다. 범위를 상태 전달 경계로 좁혔고, **장식 헤어라인
   184개를 더 넣어도 결함 수가 안 움직이는 것으로 거짓 양성 0을 증명**했다
3. **`color(srgb …)`를 0–255로 오독** → `color-mix()` 결과라 채널이 0~1인데 숫자만 긁어
   밝은 시안 틴트를 거의 검정으로 읽었다. 텍스트 실패 2건이 실제로는 6.61:1 · 4.97:1

포커스 링은 `box-shadow`라 **경계 검사가 볼 수조차 없었다.** 검사를 추가하자 진짜 결함
1건이 나왔고(스크롤 영역 4개에 포커스 스타일이 아예 없었다) 동시에 내 거짓 양성 4건도
나왔다(inset 그림자를 "표시 없음"으로 버림).

### 지금 열려 있는 결함은 5건, 원인은 하나다

선택·켜짐 상태를 칠하는 `--goalie-ref-cyan-500 #00B4ED`가 흰 배경에서 스위치 트랙 2.40:1,
선택 라디오 테두리 2.19:1로 비텍스트 기준 3:1에 못 미친다. **전달 스펙값이라 바꾸지 않고
B-1과 같은 방식으로 페이지에 적어 두었다** (`button`·`iconography`·`templates`).
결정이 반대로 나면 전달 램프의 `#0098C8`(시안 600)이 3.32:1로 기준을 넘는다.

5건은 `tests/goalie-audit.spec.js`의 `OPEN` 배열에 개별로 고정돼 있다. **개수가 아니라
항목으로 고정한 이유**는, 개수만 세면 하나가 고쳐지고 다른 하나가 생겨도 총합이 그대로이기
때문이다. 고치면 `OPEN`에서 빼야 하고, 새로 생기면 게이트가 막는다.

### 테스트가 간헐적으로 죽던 문제

원인은 **fixture 확보가 메모리 압박에서 멈추는 것**이다. 가장 분명한 증거는
`multi-system.spec.js`의 테마 부트스트랩 계약 — HTTP GET 한 번에 브라우저를 전혀 쓰지
않는데 1.5분이 걸렸다. `test.setTimeout()`은 테스트 **본문**에서 호출되므로 그 앞의 fixture
설정에는 적용되지 않는다. 전역 타임아웃을 90초로 올리고 `retries: 1`을 넣었다.

**재시도는 빨간불을 감추려는 게 아니다.** 진짜 회귀는 결정적이라 두 번 다 실패하고
(게이트를 일부러 깨서 확인했다), 멈춤만 살아남으며 Playwright가 `passed`가 아니라
`flaky`로 따로 보고한다. **출력에 `flaky`가 보이면 소음이 아니라 발견이니 읽을 것.**

"Chrome 60개가 남아서 그렇다"고 한 번 오판했는데, 실행 시간을 보니 1~16일째 떠 있는
사용자 본인 브라우저였다. CPU 스핀으로 재현을 시도한 것도 실패했다 — 압박의 종류가 달랐다.

## 2. 이미 확인된 사실 — 다시 조사하지 말 것

### 전달 자료는 딱 두 개다

컬러 스펙 시트, 순찰 로봇 관제 화면 1장, 그리고 버튼 SVG 익스포트.
페이지는 15개다. 근거가 있는 곳과 없는 곳이 갈린다.

| 근거 | 페이지 |
|---|---|
| 컬러 스펙 | colors — **값이 정확히 일치** (`--goalie-ref-*` 7개 전부) |
| 관제 화면 | button · status · input · navigation · patrol · video · templates · iconography |
| **근거 없음** | typography · spacing · principles |

### 토큰 전사는 신뢰할 수 있다

`--goalie-ref-cyan-500 #00B4ED` · `cyan-300 #31D3FF` · `cyan-400 #00C5FB` ·
`cyan-600 #0098C8` · `cyan-disabled #B3EEFE` · `red-500 #FF3E31` ·
`green-500 #4FDC5D` · `yellow-500 #FEDB25` — 모두 전달값과 일치.

### 문제가 아닌 것들 (확인 완료)

- **알럿 종 수** — 스펙이 info·danger 2종이고 구현도 2종. 일치한다.
  "System 4색이니 4종이어야 한다"는 오판이었다
- **알럿 이력 패널** — `rgba(0,0,0,0.7)` 어두운 패널 + 흰 텍스트로 정상 구현
- **한국어 줄바꿈** — `word-break: keep-all`이 15페이지 전부 적용됨
- 중복 id 0 · 제목 순서 건너뜀 0 · 320~1440px 오버플로 0 · 텍스트 대비 결함 0

## 3. 남은 작업 — 우선순위

### A. 설계 결정이 필요 (사용자 확인 후 착수)

**A-1. 빨강을 비상 전용으로 되돌리고 기본 액션을 시안으로**

전달 관제 화면에서 빨강 버튼은 `비상모드`다. 그런데 같은 `#FF3E31`이 일반 채움 버튼
색이기도 하다. 운영자가 "빨강 = 비상·정지"라는 단일 신호를 잃는다. Material·HIG·Toss 모두
error 색을 파괴적 액션에만 예약한다. **대비 문제보다 심각한 의미 체계 문제다.**
버튼 색 체계가 바뀌므로 사용자 승인이 필요하다.

**A-2. 포커스 상태 신설**

전달 SVG에 Default·Hover·Pressed·Disabled는 있으나 **포커스가 없다.** WCAG 2.2 요구이고
장갑·키보드 조작 환경에서는 필수다. 전달 근거가 없으므로 만들면 "근거 없이 채운 것"이 되고,
그 사실을 페이지에 표시해야 한다 (Parkie `motion`의 "(제안 · 확정 대기)" 방식).

**A-3. 로딩 상태 신설**

`주행 재개`는 로봇을 실제로 움직인다. 대기 표시가 없어 운영자가 두 번 누른다. 로봇 제어에서
중복 명령은 사고로 이어진다. A-2와 같은 근거 표시 필요.

### B. 전부 닫혔다 (2026-08-04)

**B-1부터 B-5까지 남은 것이 없다.** 아래는 각 항목이 어떻게 닫혔는지의 기록이고,
같은 종류의 항목을 새로 적을 사람을 위한 것이다.

| | 결과 | 커밋 |
|---|---|---|
| B-1 | 고칠 게 없었다 (문서 오기) | `92db176` |
| B-2 | `opacity: 0.3` → 전용 토큰 + 게이트 | `63fe7c3` |
| B-3 | 채움은 그대로, 눌리는 상태에 경계 추가 + 게이트 | `1cec34f` |
| B-4 | 고칠 게 없었다 (문서 오기) | `31e4eb9` |
| B-5 | 전달 표면을 레이어로 다시 씀 + 게이트 | `e1b4390` |

**다섯 중 둘이 문서 오기였다.** 원인은 두 번 다 같다 — 상태를 렌더해서 재지 않고 토큰
값만 눈으로 읽어 잉크와 채움을 헷갈렸다. 새 항목을 적을 때는 **잰 셀렉터와 잰 속성**
(`color`인지 `background-color`인지 `border-color`인지)까지 같이 적을 것. 그리고 착수
전에 다시 재라. 재는 방법은 상태를 실제로 렌더해 `getComputedStyle`로 읽는 것이고,
`node tests/tools/goalie-audit.js`가 그 방식이다.

**나머지 셋은 전부 전달값을 하나도 바꾸지 않고 닫혔다.** 이게 우연이 아니라 방법이다.
값을 바꾸기 전에 그 값이 **어떤 규칙의 결과인지 역산**해 보면, 대개 전달자가 규칙을
갖고 있었고 결과만 납품한 것으로 드러난다. 세 번 다 그랬다 (§ 아래 각 항목 참고).

**B-1. 완료 (`92db176`)** — 고칠 게 없었다. 이 문서는 "채움을 어둡게 해야 풀린다"고 적었지만
`components/goalie.css`가 그 상태에만 잉크를 순수 검정으로 내리고 있어 이미 4.67:1이었다.
원래 근거가 잉크 후보를 흰색(4.49)과 `#1C2126`(3.61) 둘만 재고 순수 검정을 재보지 않았다.
페이지 문구를 사실에 맞췄고, 게이트는 산문 대신 **빌드에서 직접 대비를 잰다.**

**B-2. 완료 (`63fe7c3`)** — `opacity: 0.3`이 라벨까지 흐렸다. 실측은 primary
outlined/text **1.63:1**, danger contained **1.81:1**, danger outlined/text **1.69:1**
(문서가 적었던 "1.3:1 수준"보다 높지만 결론은 같다). 범위는 문서보다 **좁았다** —
primary contained는 이미 전용 토큰으로 이관돼 4.17:1이었고, `opacity`가 남은 곳은
primary outlined/text와 danger 전체뿐이었다.

**색을 지어내지 않았다.** 전달된 `--goalie-ref-cyan-disabled #B3EEFE`를 역산하니 자기
enabled 채움 `#00C5FB`의 **정확히 30% 틴트**였다. 같은 30%를 빨강에 적용하면 `#FFC5C1`이
나오는데, **그건 `opacity: 0.3`이 이미 칠하고 있던 바로 그 색이다.** 채움은 원래 맞았고
잉크만 망가져 있었다. 잉크만 옮겨 3.53:1(danger contained) · 5.29:1(배경이 그대로
비치는 변형)이 됐다. 파생값이므로 `--goalie-ref-*`(= 전달됨) 이름공간 밖에 두었다.

**B-3. 완료 (`1cec34f`) — 단, 문서가 분류한 방식으로는 아니다.** 실측에서 세 숫자
`3.51 → 2.93 → 4.49`는 그대로 재현됐고, 같은 역전이 primary에도 있었다
(`2.02 → 1.73 → 2.75`). 범위는 문서보다 **넓었다.**

**"결정 없이 진행 가능"이 아니었다.** 호버 값 두 개가 전달 실측치이고,
`tests/tools/goalie-audit.js` 헤더가 *"채움 버튼이 3:1 경계를 따로 가져야 하는지는
1.4.11의 살아 있는 해석이고, 이 스크립트가 한쪽을 단정할 문제가 아니다"* 라고 이미
적어두었다. 채움을 어둡게 하는 것은 그 논쟁을 일방적으로 닫으면서 전달색 두 개를
덮어쓰는 일이 된다.

**그래서 채움을 건드리지 않고 경계를 더했다.** 채움 버튼은 테두리를 자기 채움색으로
칠하므로 잃을 경계가 없다. 호버와 Pressed에만 같은 램프의 어두운 끝(빨강 600 4.49:1,
시안 600 3.32:1)을 테두리로 준다. 테두리 상자는 원래 투명으로 잡혀 있어 **리플로가
없고**, deviation 노트가 인용하는 숫자가 **하나도 바뀌지 않는다** — `2.93`·`5.54`
포함. 채움을 어둡게 했다면 그 숫자들과 그것을 요구하는 게이트가 같이 깨졌을 것이다.

Pressed에도 넣은 이유: 호버만 고치면 시안 호버 3.32가 시안 Pressed 2.75보다 또렷해져
**역전이 사라지지 않고 한 칸 옮겨간다.**

게이트는 색이 아니라 **성질**을 잰다 — 채움과 경계 중 큰 값이 enabled → hover → pressed
순서로 **줄어들면 안 된다.** 램프를 다시 조율하는 것은 자유롭게 두고 역전만 막는다.
페이지에는 `data-spec-edge`로 세 번째 departure를 적었다. 앞의 둘과 달리 이건
**해석이 아니라 추가**다 — 전달 시트에 채움 버튼 테두리는 없다.

**B-4. 완료 — 고칠 것이 없었다 (8월 4일 실측).** 이 항목은 `#FF3E31` on 흰배경 3.51:1,
`#E03327` on `#F9D6D4` 3.34:1을 근거로 "본문 기준 미달"이라 적었다. **그 두 값은 라벨
잉크가 아니라 채움·테두리 색이다.** 아웃라인·고스트의 라벨 잉크는 `--goalie-danger-text`
= `#B42318`이고, `git log -S B42318`로 확인하니 최초 빌드 커밋 `cb54bd8`부터 그 값이었다.
작성 시점에도 이미 통과하고 있었다는 뜻이다. 실측은 outlined enabled **6.57:1**,
text pressed(`#F9D6D4` 위) **4.88:1** — 둘 다 4.5:1을 넘는다.

B-1에 이어 두 번째 같은 오기다. **두 번 다 원인이 같다** — 상태를 실제로 렌더해서 재지
않고 토큰 값만 눈으로 읽어 잉크와 채움을 헷갈렸다. 다음에 이런 항목을 적을 때는 잰
셀렉터와 잰 속성(`color`인지 `background-color`인지)까지 같이 적을 것.

**B-5. 완료 (`e1b4390`) — departure가 아예 아니었다.** 고스트 pressed의 `#F9D6D4`
불투명 채움은 접근성이 아니라 일관성 문제였다 (그 상태 라벨은 4.88:1로 통과한다).
전달값이라 덮어쓰면 안 될 것처럼 보였는데, **역산해보니 이미 상태 레이어였다.**

채널별로 풀면 `#F9D6D4`는 `--goalie-ref-red-600`의 **20%**, `#FFECEB`는
`--goalie-ref-red-400`의 **12%**이고, 채널 간 편차가 각각 0.007과 0.002다. 우연이 아니라
**전달자가 레이어로 설계해놓고 합성된 결과만 납품한 것**이다. 다시 알파로 쓰면 흰
배경에서는 전달색과 픽셀 단위로 같고, 흰 배경이 아닌 곳에서만 제대로 합성된다.
바로 그 두 번째가 결함이었다.

문서는 pressed만 적었지만 **hover도 같이 바꿨다.** 같은 결함이고, 하나만 두면 흰 배경이
아닌 곳에서 고스트가 자기 자신과 어긋난다.

**시안은 일부러 안 바꿨다.** 시안 pressed 표면은 어떤 단일 알파로도 채널 편차가
0.087이라 자기 채움의 한 레이어가 아니다. 억지로 맞추면 패턴을 예쁘게 만들려고 전달색을
바꾸는 셈이 된다. 그쪽은 리터럴로 남겼고 토큰 주석에 이유를 적었다.

### 값을 바꾸기 전에 역산부터 하라 — 이번에 세 번 통했다

B-2·B-3·B-5가 전부 전달값을 **하나도 바꾸지 않고** 닫혔다. 공통 방법은 하나다:

| 항목 | 역산한 것 | 결과 |
|---|---|---|
| B-2 | `#B3EEFE`는 무엇의 몇 %인가 | enabled 채움의 30% → 빨강에 같은 규칙 적용 |
| B-5 | `#F9D6D4`는 무엇의 몇 %인가 | pressed 채움의 20% → 알파로 되돌림 |
| B-3 | 전달 램프가 왜 호버에서 밝아지나 | 램프는 그대로 두고 경계를 더함 |

전달자는 대개 **규칙을 갖고 있었고 결과만 납품한다.** 규칙을 찾으면 전달값을 존중하면서
문제를 고칠 수 있고, 못 찾으면 그때가 진짜 departure를 적을 때다.

### C. 감사 도구 정비 — 완료 (`a8a7c3b` · `cc83a21`)

세 단계 다 끝났다. 자세한 것은 §1의 "감사 도구를 믿어도 되는 이유"와
`tests/tools/goalie-audit.js` 헤더에 있다. 결과만:

- 범위를 **상태 전달 경계**로 좁혔고, 장식 헤어라인 184개를 더 넣어도 결함 수가 안 움직이는
  것으로 **거짓 양성 0을 역방향 변이로 증명**했다
- `.gl-badge`는 **재판정해 제외**했다. 테두리는 2.40:1이지만 배지 종류가 색뿐 아니라
  텍스트로도 구분되므로("Figma 근거 고정" / "접근성 보정 명시") 라벨 주변 장식이다
- `tests/goalie-audit.spec.js`로 편입했고 변이 6종으로 검증했다

**변이 테스트가 게이트 자신의 구멍을 잡았다.** 포커스 결함을 고치자 그 항목이 `OPEN`에서
빠졌고, 그러자 포커스 검사를 통째로 지워도 모든 단정이 초록이었다. 방금 성공한 바로 그
지점에서 게이트가 게이트를 그만둔 것이다. 지금은 검사가 **실제로 돌았는지**(117개 컨트롤)도
단정한다. 새 검사를 넣을 때 같은 함정을 조심할 것.

## 4. 검증 절차 (`DESIGN_SYSTEM_AUDIT.md` 11·12절과 동일)

```
node tests/server.js &

npm test  ×3                                    계약 (릴리스 기준)
node tests/tools/route-baseline.js before       변경 전 기록
#   … 변경 …
node tests/tools/route-baseline.js after
node tests/tools/route-baseline.js --diff before after
```

`route-sweep.spec.js`는 따로 돌릴 필요가 없다. `testDir: './tests'`라서 `npm test`가
8개 스펙 87개를 전부 실행하고 route-sweep도 그 안에 있다.

배포 후 라이브에서 확인하고 **배포 커밋 = 통과 커밋**을 지킨다.

## 5. 게이트를 추가할 때의 규칙

**통과하는 테스트는 그 자체로 증거가 아니다.** 이 저장소에서 이 방법으로 무력한 단정 3건을
찾아냈다 — 포커스 링이 폭만 재서 규칙을 지워도 통과했고, 로빙 탭이 이름과 달리 `tabindex`를
보지 않았고, 플릿 검사가 `scrollWidth >= clientWidth`로 정의상 항상 참이었다.

새 게이트마다 **지키겠다는 대상을 파괴해 실패를 확인**한다. 확인하지 못한 단정은 아직
테스트가 아니다.

**변이 조준 주의** — 변이 다수가 첫 시도에 빗나갔다. `display:none`은 `toHaveCount`를
바꾸지 않고, `alt=""`는 유효한 장식 이미지이며, `hint-placeholder-count`는 에디터 힌트일 뿐
데이터가 아니다. **살아남으면 먼저 조준을 의심하고, 재조준 후에도 살아남을 때 게이트를
강화한다.**

## 6. 함께 알아야 할 것

- **병행 작업이 있다.** 이 저장소는 여러 작업자가 동시에 편집한다. 각 단계 전
  `git fetch` + `git status`로 확인하고 커밋을 미루지 말 것. 미완성 상태가 배포된 전례가 있다
- **커밋 = 배포.** 빌드 단계가 없고 Pages가 `main` 루트를 서빙한다. 공개돼도 되는 것만 커밋
- **커밋 신원** — `designerkei <239721783+designerkei@users.noreply.github.com>`.
  회사 이메일이 들어가지 않게 유지. **전역 설정은 회사 이메일일 수 있다** — 8월 4일에
  이 PC가 그랬다. 새 클론마다 아래를 먼저 실행하고 첫 커밋 전에 `git config user.email`로
  확인할 것. 한 번 푸시되면 공개 저장소에서 지울 수 없다

  ```
  git config --local user.name  "designerkei"
  git config --local user.email "239721783+designerkei@users.noreply.github.com"
  ```
- **제품 추가·제거**는 레지스트리 1항목 + 토큰 파일 + `@import` + META/NAV.
  자세한 것은 `PARKIE_MERGE_PLAN.md` 14절
- 새 PC에서의 환경 복구·저장소 밖 의존성은 §0 참고

## 7. 착수 순서 제안

```
   (완료) 3adc091..a275d71 검증          2026-08-02
   (완료) B-1                            이미 해결돼 있었고 문구만 틀렸다
   (완료) C 감사 도구 정비 + 게이트 편입
   (완료) C가 낸 목록 처리 — 20건 → 5건
   (완료) 남은 5건(시안 스펙값) 문서화     B-1과 같은 방식

   (완료) B-2~B-5 재실측                 2026-08-04. B-4는 근거가 사라져 닫힘
   (완료) B-2 · B-5 · B-3               전달값을 하나도 바꾸지 않고 닫힘

   (완료) 푸시·배포                      d4aef26까지 origin/main 반영

1. A-1~A-3   ← 설계 결정, 사용자 승인 후. 8월 4일 시점 전부 보류 상태
2. wip 브랜치 처리 — 새 도형 7종을 가져올지 결정 (§8)
3. 영어 번역 구멍 — 게이트가 15페이지 중 2개만 본다 (§1 참고)
```

각 단계를 `npm test` 3회 + 커밋 + 배포로 닫아, 중간에 끊겨도 공개 상태가 망가지지 않게 한다.

**A를 착수할 때 B에서 통한 방법을 먼저 써볼 것.** A-1~A-3은 셋 다 "전달 근거가 없으니
새로 만들어야 한다"로 적혀 있는데, B에서도 셋이 그렇게 보였다가 역산하니 전달자가 규칙을
갖고 있었다. 특히 A-2 포커스는 전달 SVG에 없지만, 다른 상태들이 램프와 레이어 규칙을
따르고 있으므로 **같은 규칙에서 유도할 수 있는지부터** 확인하는 것이 "근거 없이 채운 것"을
하나 줄이는 길이다.

**시작하기 전에 이것부터:**

```
node tests/server.js &
node tests/tools/goalie-audit.js     # 지금 무엇이 열려 있는지. 지금은 5건
npm test                             # 87개. flaky 줄이 보이면 읽을 것
```

## 8. 보류 중인 브랜치 — `wip/iconography-rewrite-2026-08-03`

8월 3일에 revert한 아이콘 페이지 재작성이 그대로 들어 있다. **통째로 되살리면 안 된다** —
`main`을 망가뜨렸던 그 커밋이고, 그때 없던 상태 시트·획 통일·토큰 수정이 지금 `main`에 있다.

살릴 값어치가 있는 것만 추려두었다. `PARKIE_ICONS` 25항목을 `main` 및 `ms/icon-data.js`의
path와 대조한 결과:

| 분류 | 항목 |
|---|---|
| **새 도메인 도형 7종** | `park-in` · `park-out` · `slot-map` · `queue` · `maintenance` · `power` · `control-settings` |
| MS 복사본 | `location` · `settings` · `restart` · `lock` — `ms/icon-data.js`에 이미 있다 |
| main과 동일 | `monitoring` · `emergency-stop` · `robot-status` · `charge` |
| 기존 채택본과 경합 | `home` · `camera` · `alert` · `profile` · `play` · `pause` — MS 채택본을 다시 그린 것 |
| 미완성 | `stop` · `navigation` · `robot-parking` · `control` — path가 없다 |

**가져올 것은 새 도형 7종뿐이다.** 가져오려면 도형만 `icons/parkie-icon-data.js`의
`export default` 객체에 넣고(브랜치는 named export로 바꿔놨는데 그게 로드를 깨뜨린 원인 중
하나다), 페이지 행과 테스트를 함께 추가한다. 데이터 파일만 따로 가져오면 즉시 깨진다.

되살릴 계획이 없다고 판단되면 브랜치를 지워도 된다 — 위 표가 그 안에 뭐가 있었는지의 기록이다.
