# @designerkei/parkie-ui

This pre-release package exists only to prove the private registry publication
and consumer installation path. It does not contain the Parkie design tokens or
React components yet.

Do not use `0.0.0-registry-smoke.*` in product code. The first product-facing
contract will start at `0.1.0` after the registry gate passes.
